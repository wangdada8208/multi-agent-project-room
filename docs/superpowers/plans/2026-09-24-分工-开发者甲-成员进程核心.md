# 开发者甲任务书：成员进程核心

> 两人分工中的一份。另一份是 `docs/superpowers/plans/2026-09-24-分工-开发者乙-Hub前端与控制台.md`，由开发者乙执行。
> 本文把原阶段计划中属于你的任务原样放在一起，只额外加了分工说明和"等待点"。任务正文、代码、命令、预期输出都没有改。
> 唯一改动：任务里 `git add` 原计划文件的地方，改成了 `git add` 本文件。

## 0. 你负责什么

**负责的文件：**

- `workers/xmtp-member/src/` 下除以下 7 个文件以外的全部文件：`outgoingRequest.ts`、`outgoingRequest.test.ts`、`localGuard.ts`、`localGuard.test.ts`、`ownerConsole.ts`、`ownerConsole.test.ts`、`ownerConsoleHtml.ts`（这 7 个归开发者乙）。
- `workers/xmtp-member/src/index.ts`
- `workers/xmtp-member/package.json`、`workers/xmtp-member/package-lock.json`、`workers/xmtp-member/.env.example`
- `ACCEPTANCE_CHECKLIST.md` 只在阶段 B Task 14 Step 8 加一行。
- 本文件的勾选框。

**不碰：** `backend/`、`frontend/`、`CLAUDE.md`、`PLAN.md`、`ARCHITECTURE.md`、`RUNBOOK.md`。

**双机演练时你是"甲机"**，也就是房主那台。

**你的任务一览：**

| 阶段 | 任务 |
| --- | --- |
| A | Task 3 本机账本、Task 4 发送者白名单、Task 5 群复用、Task 5b 缺少的成员、Task 6 接进入口、Task 10 双机演练（甲机部分） |
| B | Task 2 viem 依赖、Task 3 消息编解码、Task 4 单次授权、Task 5 虚构日历、Task 6 范围策略、Task 7 同意队列与收件箱、Task 8 处理消息与批准、Task 12 接进入口、Task 14 单机双网关实测 |
| 后续 | 阶段 C 协调者与计分板、阶段 D 真实连接器（都要先展开） |

## 1. 两人协作规则

1. **只改自己负责的文件。** 第 0 节列出了你负责的文件。别人的文件哪怕发现问题，也不要改，告诉你的人类，由对方处理。
2. **共用一个分支 `feat/owner-gateway`。** 开发者乙在拿到阶段 A 审批后创建并推送这个分支。
3. **开始每个任务前，先拉取对方的最新提交：**

```bash
git pull --rebase origin feat/owner-gateway
```

4. **每个任务的提交步骤做完后，立刻推送：**

```bash
git pull --rebase origin feat/owner-gateway && git push origin feat/owner-gateway
```

5. **拉取时出现冲突：** 执行 `git rebase --abort`，停止工作，告诉你的人类是哪个文件冲突，由两个人商量。两人负责的文件不重叠，正常情况下不会冲突。
6. **遇到"等待点"：** 按等待点里的命令检查。条件不满足就停下，告诉你的人类"在等对方完成某某"，不要自己去做对方的任务。
7. **勾选进度只在本文件里做。** 原来的阶段计划文件不要再勾选，避免两人同时改同一个文件。
8. **任务里写着"发给人类，等待回复"的步骤，只由开发者乙执行。** 开发者甲看到审批关卡时，按对应等待点检查结果。
9. **任务编号保持原样**（例如"阶段 A Task 6"），和原计划、评估报告里的引用一致。

## 2. 执行顺序与汇合点

| 顺序 | 做什么 | 要等对方吗 |
| --- | --- | --- |
| 1 | 等待点 A0：分支已存在（代表阶段 A 已获人类批准） | 等乙 |
| 2 | 阶段 A Task 3、4、5、5b、6 | 不等 |
| 3 | 等待点 A10：乙的阶段 A Task 2、7 已推送 | 等乙 |
| 4 | 阶段 A Task 10 双机演练，和乙一起做 | 一起 |
| 5 | 等待点 B0：阶段 B 已获批准 | 等乙 |
| 6 | 阶段 B Task 2、3，**做完 Task 3 立刻推送**（乙在等 `envelope.ts`） | 不等 |
| 7 | 阶段 B Task 4、5、6、7，**做完 Task 7 立刻推送**（乙在等 `consentQueue.ts`、`inbox.ts`） | 不等 |
| 8 | 阶段 B Task 8 | 不等 |
| 9 | 等待点 B12：乙的控制台相关文件已推送 | 等乙 |
| 10 | 阶段 B Task 12、14 | 不等 |
| 11 | 告诉你的人类"阶段 B 甲的部分完成"，由乙做文档和合并询问 | — |

---

## 3. 通用执行规则（原文）

以下是 `docs/superpowers/plans/2026-09-23-00-总路线与执行规则.md` 的全文。其中"按 Task 编号顺序做"指你自己这份里的顺序。

# 总路线与执行规则

> 这是完成整个项目的入口文档。任何代理（包括能力较弱的模型）接手时，先读完这一份，再打开下面列出的阶段计划。

---

## 1. 评估报告能不能直接交给较弱的模型实现？

不能。`docs/评估报告-对标AgentPicnic与架构演进方案.md` 是一份评估和提案，适合人类做决定，不适合照着写代码。它缺三样东西：

1. **没有逐行代码和测试。** 报告只说"成员进程加发送者白名单""定义内容类型"，没有写函数名、参数、返回值和测试断言。较弱的模型会自己发明接口，前后对不上。
2. **没有运行命令和预期输出。** 执行者无法判断自己做对了没有。
3. **有未验证的假设。** 写报告时没有发现"另一方的浏览器和成员进程没人拉进群"。按报告做，双机演练必然失败。

所以另外写了下面这套计划。

## 2. 计划清单与执行顺序

| 顺序 | 文件 | 性质 | 能否交给较弱的模型 |
| --- | --- | --- | --- |
| 1 | `docs/superpowers/plans/2026-09-23-A-恢复生产与安全修复.md` | 完整计划，含全部代码 | 能。Task 9、10 需要人类配合 |
| 2 | `docs/superpowers/plans/2026-09-23-B-主人网关与单次同意.md` | 完整计划，含全部代码 | 能 |
| 3 | `docs/superpowers/plans/2026-09-23-C-E-后续阶段任务书.md` | 任务书，含接口与测试清单 | 不能直接执行。先由较强的模型或人类展开成完整计划，展开规则写在该文件第 1 节 |

阶段 A、B 的每一段代码，都已在 2026-09-23 的临时副本里跑过：
- 后端 90 项测试通过；
- 成员进程 85 项测试通过，类型检查干净；
- 前端 22 项测试通过，构建通过；
- 成员进程在 XMTP `dev` 网络上用三个临时身份实测过：
  - 请求、等待批准、允许一次、只收到时段；
  - 越界请求自动拒绝；
  - 复用已有群时只补加缺少的成员；
  - 非白名单发送者的消息只进账本。

照抄就能通过。如果照抄后测试不过，先检查是不是抄错了，再按第 7 节排查。不要改测试去迁就代码。

## 3. 执行规则（每一条都必须遵守）

1. **一次只做一个任务。** 按计划里的 Task 编号顺序做，不跳，不合并。
2. **每一步都要执行并对照 Expected。** 输出和 Expected 不一致时，不要继续下一步。
3. **先测试后实现。** 计划要求"运行，确认失败"的步骤不能省。它证明测试真的在测新东西。
4. **代码照抄。** 计划里给了完整代码的地方，一个字符都不要改，包括注释和空行。
5. **只改计划里列出的文件。** `Files:` 之外的文件不要碰。
6. **每个任务结束都提交**，提交信息照计划里写的格式，这是 `AGENTS.md` 第 6 节的要求。
7. **勾选进度。** 每完成一步，把计划文件里对应的 `- [ ]` 改成 `- [x]`，随该任务一起提交。下一位接手的人靠这个知道做到哪了。
8. **不要把密钥、地址、群 id、令牌、`.env`、`.state/` 目录写进 git、提交信息或聊天。**

## 4. 必须停下来问人类的情况

遇到下面任何一种情况，停止工作，把情况原样告诉人类，等回复：

| 情况 | 原因 |
| --- | --- |
| 计划里写着"人类审批关卡" | `AGENTS.md` 规则 3 |
| 要合并或推送 `main` | 同上 |
| 要部署生产、登录服务器、连接生产库 | 同上 |
| 要改数据库表结构 | 同上 |
| 同一步骤按排查表试过仍然失败 | 避免越改越乱 |
| 需要改计划没有列出的文件 | 可能是计划有缺口，要让人类或较强的模型补计划 |
| 需要真实的个人数据或第三方账号 | 阶段 D 的前提 |

## 5. 环境准备（第一次接手时做一次）

```bash
cd /Users/wangdada/Desktop/A2A-agent连接

# Python 后端
python3.12 -m venv .venv
.venv/bin/pip install -r backend/requirements.txt
cd backend && ../.venv/bin/python -m pytest -q && cd ..

# 成员进程（需要 Node 22 或更高）
node --version
cd workers/xmtp-member && npm install && npm test && cd ../..

# 前端
cd frontend && npm install && npx vitest run && npm run build && cd ..
```

Expected（阶段 A 开始前）：后端 `87 passed`；成员进程 `ℹ pass 27`；前端 `Tests  13 passed`，构建成功。

数字不一致，说明仓库状态和计划写作时不同。先 `git log --oneline -5` 看最新提交，再问人类。

## 6. 名词解释

| 名词 | 意思 |
| --- | --- |
| Hub | `backend/` 里的 FastAPI 服务。管账号、房间、成员、群绑定。加密房间的聊天内容它读不到 |
| 成员进程 / 主人网关 | `workers/xmtp-member`。跑在每个主人自己的电脑上，持有 XMTP 私钥，替主人收发和把关 |
| XMTP | 端到端加密的消息网络。`dev` 是测试网络，可以随便用；`production` 是正式网络 |
| 群 / group | XMTP 里的一个加密群聊。Hub 房间通过 `xmtp_group_id` 对应一个群 |
| 白名单 | `XMTP_ALLOWED_SENDERS`。只处理这些地址发来的消息 |
| 策略 | `.state/policy.json`。谁能向我申请哪类数据 |
| 同意队列 | `.state/consents.json`。等主人批准的申请 |
| 授权 / Grant | 主人批准后签发的单次凭据，10 分钟有效，绑定接收人和数据哈希 |
| 账本 | `.state/ledger.jsonl`。只记"什么时候、和谁、发生了什么"，不记内容 |
| 控制台 | 成员进程在 `http://127.0.0.1:8787/` 提供的网页 |
| MAPR1 | 群里结构化消息的前缀 |

## 7. 常见问题排查

| 现象 | 原因与处理 |
| --- | --- |
| `Cannot find module '.../xxx.ts'` | 还没创建该文件，或文件名大小写不对。对照计划的 `Files:` |
| 后端测试 `ModuleNotFoundError` | 没用 `.venv` 里的 Python。用 `../.venv/bin/python -m pytest` |
| `npm run typecheck` 报 `allowImportingTsExtensions` | `package.json` 的 `typecheck` 脚本没照抄。见阶段 A Task 6 Step 1 |
| 成员进程启动报 `XMTP_ALLOWED_SENDERS is empty` | 这是故意的。在 `.env` 里填至少一个地址 |
| 成员进程启动报 `EADDRINUSE` | 端口被占用。换 `OWNER_NOTES_PORT`，或关掉上一个成员进程 |
| 日志 `group ... not found locally` | 这个成员还没被拉进群。等已在群里的成员进程补加后重启 |
| 绑定群返回 403 | 当前登录用户不是房主。换房主账号 |
| 绑定群返回 409 | 房间已经绑定了另一个群。这是故意的，不要去改数据库 |
| 控制台 POST 返回 403 | 没带 `X-MAPR-Owner: 1` 请求头，或者不是从 127.0.0.1 访问 |
| 请求发出后对方控制台没有待批准 | 看对方账本：`sender_dropped` 说明你不在对方白名单；`request_denied` 说明对方策略不允许 |
| 批准后对方收件箱为空 | 看对方账本：`disclosure_rejected` 后面的 `reason` 说明了原因；`sender_dropped` 说明批准方不在接收方白名单 |

## 8. 进度一览

每次完成一个阶段，更新下表并提交。

| 阶段 | 状态 | 完成日期 | 备注 |
| --- | --- | --- | --- |
| A 恢复生产与安全修复 | 未开始 | | |
| B 主人网关与单次同意 | 未开始 | | |
| C 协调者与计分板 | 未展开 | | |
| D 真实连接器 | 未展开 | | 需要人类单独批准 |
| E 代理自助接入 | 未展开 | | |

---

## 4. 阶段 A：你的任务

以下先是原阶段 A 计划的开头部分（目标、开始前必读、不要做），再是你的任务。

# 阶段 A：恢复生产与安全修复 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修掉加密房间的三个安全问题（任意成员改绑群、成员进程不校验发送者、重启就新建群），把 XMTP 网络改成可配置，清理仓库卫生；在人类恢复服务器登录后恢复生产并完成双机演练。

**Architecture:** Hub 的 `/xmtp-binding` 改为只有房主能绑、绑定后不可覆盖。成员进程启动时读取 `XMTP_ALLOWED_SENDERS`，只处理名单内发送者的消息，其他消息只在本机账本 `ledger.jsonl` 记一行。成员进程先问 Hub 房间是否已有群，有就复用。

**Tech Stack:** FastAPI + pytest（`backend/`），Node 22 + `node --test`（`workers/xmtp-member/`），React + Vitest（`frontend/`）。

**本计划的每段代码都已在 2026-09-23 的临时副本里跑过：后端 90 项、成员进程 39 项、前端 18 项测试通过，前端构建通过，成员进程类型检查通过；成员进程入口还在 XMTP `dev` 网络上用三个临时身份实测过。照抄即可。**

---

## 开始前必读

1. 先读 `docs/superpowers/plans/2026-09-23-00-总路线与执行规则.md`。里面有环境准备、提交格式和"什么时候必须停下来问人"。
2. 本计划 Task 1 是人类审批关卡。没有拿到人类的明确同意，不要做 Task 2 以后的任何改动。
3. 所有命令都从仓库根目录 `/Users/wangdada/Desktop/A2A-agent连接` 开始执行，除非步骤里写了 `cd`。
4. 不要改本计划没有提到的文件。

**不要做：**

- 不合并 `main`，不推送 `main`。
- 不连接、不修改生产库。Task 9 只有在人类恢复服务器登录后才做，并且严格按旧计划执行。
- 不把密钥、`frontend/.env.local`、`.state/` 目录下的任何文件提交进 git。

---

---

### 等待点：A0 分支已存在

```bash
git fetch origin
git branch -r | rg "origin/feat/owner-gateway"
```

- 有输出：执行 `git checkout -b feat/owner-gateway origin/feat/owner-gateway`。如果本地已有这个分支，改用 `git checkout feat/owner-gateway && git pull --rebase origin feat/owner-gateway`。然后继续。
- 没有输出：乙还没拿到人类的阶段 A 批准。停下等待。

阶段 A Task 1、2、7、8、9、11 由开发者乙执行，本文不包含。

---

### Task 3: 本机账本

成员进程需要一个只记元数据的本机账本，后面的发送者拦截和阶段 B 的同意流程都往这里写。

**Files:**
- Create: `workers/xmtp-member/src/ledger.ts`
- Create: `workers/xmtp-member/src/ledger.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/ledger.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { appendLedger, buildLedgerEntry, readLedger } from "./ledger.ts";

test("ledger entry has only metadata fields", () => {
  const entry = buildLedgerEntry({
    now: new Date("2026-10-01T00:00:00Z"),
    kind: "sender_dropped",
    peer: "0xABCDEFabcdefABCDEFabcdefABCDEFabcdefABCD",
    reason: "sender_not_allowed",
  });
  assert.deepEqual(Object.keys(entry).sort(), [
    "at", "kind", "payload_hash", "peer", "reason", "request_id", "scope",
  ]);
  assert.equal(entry.peer, "0xabcdefabcdefabcdefabcdefabcdefabcdefabcd");
  assert.equal(entry.request_id, null);
});

test("append then read round-trips and skips broken lines", async () => {
  const dir = await mkdtemp(path.join(os.tmpdir(), "ledger-"));
  const file = path.join(dir, "ledger.jsonl");
  assert.deepEqual(await readLedger(file), []);
  const e = buildLedgerEntry({ now: new Date("2026-10-01T00:00:00Z"), kind: "request_sent", requestId: "r1" });
  await appendLedger(file, e);
  const { appendFile } = await import("node:fs/promises");
  await appendFile(file, "not json\n", "utf8");
  await appendLedger(file, e);
  const all = await readLedger(file);
  assert.equal(all.length, 2);
  assert.equal(all[0].request_id, "r1");
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/ledger.test.ts
```

Expected: FAIL，报错信息包含 `Cannot find module` 和 `ledger.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/ledger.ts`：

```ts
import { existsSync } from "node:fs";
import { appendFile, mkdir, readFile } from "node:fs/promises";
import path from "node:path";

export type LedgerKind =
  | "sender_dropped"
  | "request_sent"
  | "request_denied"
  | "request_queued"
  | "consent_approved"
  | "consent_denied"
  | "consent_expired"
  | "send_failed"
  | "disclosure_stored"
  | "disclosure_rejected"
  | "denial_received"
  | "pending_received";

export interface LedgerEntry {
  at: string;
  kind: LedgerKind;
  request_id: string | null;
  peer: string | null;
  scope: string | null;
  reason: string | null;
  payload_hash: string | null;
}

export function buildLedgerEntry(input: {
  now: Date;
  kind: LedgerKind;
  requestId?: string | null;
  peer?: string | null;
  scope?: string | null;
  reason?: string | null;
  payloadHash?: string | null;
}): LedgerEntry {
  return {
    at: input.now.toISOString(),
    kind: input.kind,
    request_id: input.requestId ?? null,
    peer: input.peer ? input.peer.toLowerCase() : null,
    scope: input.scope ?? null,
    reason: input.reason ?? null,
    payload_hash: input.payloadHash ?? null,
  };
}

export async function appendLedger(file: string, entry: LedgerEntry): Promise<void> {
  await mkdir(path.dirname(file), { recursive: true });
  await appendFile(file, JSON.stringify(entry) + "\n", "utf8");
}

export async function readLedger(file: string): Promise<LedgerEntry[]> {
  if (!existsSync(file)) return [];
  const content = await readFile(file, "utf8");
  const out: LedgerEntry[] = [];
  for (const line of content.split("\n")) {
    const trimmed = line.trim();
    if (!trimmed) continue;
    try {
      out.push(JSON.parse(trimmed));
    } catch {
      // skip malformed line
    }
  }
  return out;
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/ledger.test.ts
```

Expected: `ℹ pass 2`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/ledger.ts workers/xmtp-member/src/ledger.test.ts
git commit -m "$(cat <<'EOF'
[XMTP] 增加只记元数据的本机账本

- Added: ledger.ts（buildLedgerEntry、appendLedger、readLedger）
- Handoff: 账本字段固定为 at/kind/request_id/peer/scope/reason/payload_hash，不许加正文字段
EOF
)"
```

---

---

### Task 4: 发送者白名单

**Files:**
- Create: `workers/xmtp-member/src/senderPolicy.ts`
- Create: `workers/xmtp-member/src/senderPolicy.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/senderPolicy.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { isAllowedSender, parseAllowedSenders } from "./senderPolicy.ts";

const A = "0x1111111111111111111111111111111111111111";
const B = "0x2222222222222222222222222222222222222222";

test("parses a comma list, lowercases it and drops malformed entries", () => {
  const list = parseAllowedSenders(` ${A.toUpperCase().replace("0X", "0x")} , not-an-address, ${B}`);
  assert.deepEqual(list, [A, B]);
});

test("empty or missing env gives an empty list", () => {
  assert.deepEqual(parseAllowedSenders(undefined), []);
  assert.deepEqual(parseAllowedSenders(""), []);
});

test("only listed senders are allowed", () => {
  assert.equal(isAllowedSender(A, [A]), true);
  assert.equal(isAllowedSender(A.toUpperCase().replace("0X", "0x"), [A]), true);
  assert.equal(isAllowedSender(B, [A]), false);
  assert.equal(isAllowedSender(undefined, [A]), false);
  assert.equal(isAllowedSender("", [A]), false);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/senderPolicy.test.ts
```

Expected: FAIL，找不到 `senderPolicy.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/senderPolicy.ts`：

```ts
export function parseAllowedSenders(raw: string | undefined): string[] {
  return (raw || "")
    .split(",")
    .map((s) => s.trim().toLowerCase())
    .filter((s) => /^0x[0-9a-f]{40}$/.test(s));
}

export function isAllowedSender(
  sender: string | undefined | null,
  allowed: string[]
): boolean {
  if (!sender) return false;
  return allowed.includes(sender.toLowerCase());
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/senderPolicy.test.ts
```

Expected: `ℹ pass 3`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/senderPolicy.ts workers/xmtp-member/src/senderPolicy.test.ts
git commit -m "$(cat <<'EOF'
[XMTP] 增加发送者白名单解析与判断

- Added: parseAllowedSenders、isAllowedSender
- Handoff: 名单只接受 0x 开头的 40 位十六进制地址，统一转小写
EOF
)"
```

---

---

### Task 5: 复用已绑定的群，不再每次启动新建

**Files:**
- Create: `workers/xmtp-member/src/groupPlan.ts`
- Create: `workers/xmtp-member/src/groupPlan.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/groupPlan.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { fetchHubGroupId, resolveGroupPlan } from "./groupPlan.ts";

const PEER = "0x1111111111111111111111111111111111111111";

test("env group id wins over everything", () => {
  assert.deepEqual(
    resolveGroupPlan({ envGroupId: "g-env", hubGroupId: "g-hub", peerAddresses: [PEER] }),
    { action: "use", groupId: "g-env", source: "env" }
  );
});

test("hub group id is reused instead of creating a new group", () => {
  assert.deepEqual(
    resolveGroupPlan({ hubGroupId: "g-hub", peerAddresses: [PEER] }),
    { action: "use", groupId: "g-hub", source: "hub" }
  );
});

test("creates only when nothing is bound and peers are configured", () => {
  assert.deepEqual(resolveGroupPlan({ hubGroupId: null, peerAddresses: [PEER] }), { action: "create" });
  assert.deepEqual(resolveGroupPlan({ peerAddresses: [] }), { action: "none" });
});

test("fetchHubGroupId reads room.xmtp_group_id with the bearer token", async () => {
  let seenUrl = "";
  let seenAuth = "";
  const fakeFetch = (async (url: string, init: any) => {
    seenUrl = url;
    seenAuth = init.headers.Authorization;
    return new Response(JSON.stringify({ room: { id: "r1", xmtp_group_id: "g-1" } }), { status: 200 });
  }) as unknown as typeof fetch;
  const id = await fetchHubGroupId({
    hubBaseUrl: "http://hub.local/",
    hubRoomId: "r1",
    hubToken: "tok",
    fetchImpl: fakeFetch,
  });
  assert.equal(id, "g-1");
  assert.equal(seenUrl, "http://hub.local/api/v1/rooms/r1");
  assert.equal(seenAuth, "Bearer tok");
});

test("fetchHubGroupId returns null when the room is not bound yet", async () => {
  const fakeFetch = (async () =>
    new Response(JSON.stringify({ room: { id: "r1", xmtp_group_id: null } }), { status: 200 })) as unknown as typeof fetch;
  assert.equal(
    await fetchHubGroupId({ hubBaseUrl: "http://hub.local", hubRoomId: "r1", hubToken: "t", fetchImpl: fakeFetch }),
    null
  );
});

test("fetchHubGroupId throws on HTTP errors without echoing the token", async () => {
  const fakeFetch = (async () => new Response("no", { status: 403 })) as unknown as typeof fetch;
  await assert.rejects(
    fetchHubGroupId({ hubBaseUrl: "http://hub.local", hubRoomId: "r1", hubToken: "secret-token", fetchImpl: fakeFetch }),
    (err: Error) => err.message.includes("403") && !err.message.includes("secret-token")
  );
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/groupPlan.test.ts
```

Expected: FAIL，找不到 `groupPlan.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/groupPlan.ts`：

```ts
export type GroupPlan =
  | { action: "use"; groupId: string; source: "env" | "hub" }
  | { action: "create" }
  | { action: "none" };

export function resolveGroupPlan(input: {
  envGroupId?: string;
  hubGroupId?: string | null;
  peerAddresses: string[];
}): GroupPlan {
  const envId = (input.envGroupId || "").trim();
  if (envId) return { action: "use", groupId: envId, source: "env" };
  const hubId = (input.hubGroupId || "").trim();
  if (hubId) return { action: "use", groupId: hubId, source: "hub" };
  if (input.peerAddresses.length > 0) return { action: "create" };
  return { action: "none" };
}

export async function fetchHubGroupId(input: {
  hubBaseUrl: string;
  hubRoomId: string;
  hubToken: string;
  fetchImpl?: typeof fetch;
}): Promise<string | null> {
  const fetchFn = input.fetchImpl ?? fetch;
  const url = `${input.hubBaseUrl.replace(/\/$/, "")}/api/v1/rooms/${input.hubRoomId}`;
  const resp = await fetchFn(url, {
    headers: { Authorization: `Bearer ${input.hubToken}` },
  });
  if (!resp.ok) {
    throw new Error(`Failed to read hub room: HTTP status ${resp.status}`);
  }
  const data: any = await resp.json();
  const id = data?.room?.xmtp_group_id;
  return typeof id === "string" && id.length > 0 ? id : null;
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/groupPlan.test.ts
```

Expected: `ℹ pass 6`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/groupPlan.ts workers/xmtp-member/src/groupPlan.test.ts
git commit -m "$(cat <<'EOF'
[XMTP] 决定复用还是新建群的纯函数

- Added: resolveGroupPlan、fetchHubGroupId
- Handoff: 优先级固定为 XMTP_GROUP_ID > Hub 已绑定群 > 有对端地址才新建 > 只监听
EOF
)"
```

---

---

### Task 5b: 计算群里还缺哪些成员

浏览器建群时只会把一个成员进程拉进群。另一方的浏览器和成员进程要由已在群里的成员进程补进来，否则双机演练必然失败。

**Files:**
- Create: `workers/xmtp-member/src/groupMembers.ts`
- Create: `workers/xmtp-member/src/groupMembers.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/groupMembers.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { missingMembers } from "./groupMembers.ts";

const A = "0x1111111111111111111111111111111111111111";
const B = "0x2222222222222222222222222222222222222222";

test("only addresses not already in the group are returned, once each", () => {
  assert.deepEqual(missingMembers([A], [A.toUpperCase().replace("0X", "0x"), B, B]), [B]);
  assert.deepEqual(missingMembers([A, B], [A, B]), []);
  assert.deepEqual(missingMembers([], []), []);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/groupMembers.test.ts
```

Expected: FAIL，找不到 `groupMembers.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/groupMembers.ts`：

```ts
export function missingMembers(present: string[], wanted: string[]): string[] {
  const have = new Set(present.map((a) => a.toLowerCase()));
  const out: string[] = [];
  for (const raw of wanted) {
    const address = raw.toLowerCase();
    if (!have.has(address) && !out.includes(address)) out.push(address);
  }
  return out;
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/groupMembers.test.ts
```

Expected: `ℹ pass 1`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/groupMembers.ts workers/xmtp-member/src/groupMembers.test.ts
git commit -m "$(cat <<'EOF'
[XMTP] 计算群内缺少的成员地址

- Added: missingMembers
- Handoff: Task 6 用它把 XMTP_PEER_ADDRESSES 补进已有群
EOF
)"
```

---

---

### Task 6: 把白名单和群复用接进成员进程入口

**Files:**
- Modify: `workers/xmtp-member/src/index.ts`（整文件替换）
- Modify: `workers/xmtp-member/package.json`（增加 `typecheck` 脚本）
- Modify: `workers/xmtp-member/.env.example`

- [ ] **Step 1: 增加类型检查脚本**

打开 `workers/xmtp-member/package.json`，把 `"scripts"` 整段替换为：

```json
  "scripts": {
    "test": "node --experimental-strip-types --test src/*.test.ts",
    "typecheck": "tsc --noEmit -p . --allowImportingTsExtensions",
    "start": "node --experimental-strip-types src/index.ts"
  },
```

- [ ] **Step 2: 整文件替换 `workers/xmtp-member/src/index.ts`**

用下面的内容覆盖整个文件：

```ts
import { existsSync } from "node:fs";
import { appendFile, mkdir } from "node:fs/promises";
import path from "node:path";
import { Agent } from "@xmtp/agent-sdk";
import { buildBindingPayload } from "./binding.ts";
import { missingMembers } from "./groupMembers.ts";
import { fetchHubGroupId, resolveGroupPlan } from "./groupPlan.ts";
import { appendLedger, buildLedgerEntry } from "./ledger.ts";
import {
  createLocalLoop,
  loadLoopState,
  saveLoopState,
  type LocalLoopState,
} from "./localLoop.ts";
import { completeWithFetch } from "./modelComplete.ts";
import { onInboundText } from "./onInboundText.ts";
import type { OwnerNote } from "./ownerNote.ts";
import {
  createOwnerNotesServer,
  ownerNotesBindAddress,
} from "./ownerNotesHttp.ts";
import { isAllowedSender, parseAllowedSenders } from "./senderPolicy.ts";

export interface MemberConfig {
  selfName?: string;
  participants?: string[];
  xmtpWalletKey?: string;
  xmtpDbEncryptionKey?: string;
  xmtpEnv?: string;
  xmtpPeerAddresses?: string[];
  xmtpGroupId?: string;
  allowedSenders: string[];
  hubBaseUrl?: string;
  hubToken?: string;
  hubRoomId?: string;
  loopStatePath?: string;
  memberModelApiKey?: string;
  memberModelUrl?: string;
  approvedDays?: string[];
  sensitiveKeywords?: string[];
  ownerNotesPort?: string;
}

function splitList(raw: string | undefined): string[] {
  return (raw || "")
    .split(",")
    .map((s) => s.trim())
    .filter(Boolean);
}

export function loadConfigFromEnv(): MemberConfig {
  const participants = splitList(process.env.XMTP_PARTICIPANTS);
  const approvedDays = splitList(process.env.XMTP_APPROVED_DAYS);
  const sensitiveKeywords = splitList(process.env.XMTP_SENSITIVE_KEYWORDS);

  const defaultStatePath = existsSync("workers/xmtp-member")
    ? path.resolve("workers/xmtp-member/.state/loop.json")
    : path.resolve(".state/loop.json");

  return {
    selfName: process.env.XMTP_SELF_NAME || "Codex",
    participants: participants.length > 0 ? participants : ["Codex", "Claude"],
    xmtpWalletKey: process.env.XMTP_WALLET_KEY,
    xmtpDbEncryptionKey: process.env.XMTP_DB_ENCRYPTION_KEY,
    xmtpEnv: process.env.XMTP_ENV || "dev",
    xmtpPeerAddresses: splitList(process.env.XMTP_PEER_ADDRESSES).map((s) => s.toLowerCase()),
    xmtpGroupId: process.env.XMTP_GROUP_ID || undefined,
    allowedSenders: parseAllowedSenders(process.env.XMTP_ALLOWED_SENDERS),
    hubBaseUrl: process.env.HUB_BASE_URL,
    hubToken: process.env.HUB_TOKEN,
    hubRoomId: process.env.HUB_ROOM_ID,
    loopStatePath: process.env.XMTP_LOOP_STATE_PATH || defaultStatePath,
    memberModelApiKey: process.env.MEMBER_MODEL_API_KEY,
    memberModelUrl: process.env.MEMBER_MODEL_URL,
    approvedDays: approvedDays.length > 0 ? approvedDays : undefined,
    sensitiveKeywords: sensitiveKeywords.length > 0 ? sensitiveKeywords : undefined,
    ownerNotesPort: process.env.OWNER_NOTES_PORT,
  };
}

export async function appendOwnerNote(
  filePath: string,
  note: OwnerNote
): Promise<void> {
  await mkdir(path.dirname(filePath), { recursive: true });
  await appendFile(filePath, JSON.stringify(note) + "\n", "utf8");
}

export async function bindGroupToHub(
  hubBaseUrl: string,
  hubRoomId: string,
  hubToken: string,
  groupId: string
): Promise<void> {
  const payload = buildBindingPayload({ xmtpGroupId: groupId });
  const url = `${hubBaseUrl.replace(/\/$/, "")}/api/v1/rooms/${hubRoomId}/xmtp-binding`;

  const resp = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${hubToken}`,
    },
    body: JSON.stringify(payload),
  });

  if (!resp.ok) {
    throw new Error(`Failed to bind group to hub: HTTP status ${resp.status}`);
  }
}

export async function startMember(): Promise<void> {
  const config = loadConfigFromEnv();
  if (config.allowedSenders.length === 0) {
    throw new Error("XMTP_ALLOWED_SENDERS is empty; refusing to start");
  }
  const agent = await Agent.createFromEnv();
  const selfAddress = (agent.address || "").toLowerCase();

  const statePath =
    config.loopStatePath ||
    (existsSync("workers/xmtp-member")
      ? path.resolve("workers/xmtp-member/.state/loop.json")
      : path.resolve(".state/loop.json"));
  const stateDir = path.dirname(statePath);
  const ownerNotesPath = path.join(stateDir, "owner-notes.jsonl");
  const ledgerPath = path.join(stateDir, "ledger.jsonl");

  const ownerNotesServer = createOwnerNotesServer(ownerNotesPath);
  const ownerNotesPort = parseInt(config.ownerNotesPort || "8787", 10);
  ownerNotesServer.listen(ownerNotesPort, ownerNotesBindAddress());

  let loopState: LocalLoopState;
  if (existsSync(statePath)) {
    loopState = await loadLoopState(statePath);
  } else {
    loopState = createLocalLoop({
      participants: config.participants || ["Codex", "Claude"],
      selfName: config.selfName || "Codex",
    });
  }

  const complete =
    config.memberModelApiKey && config.memberModelUrl
      ? (prompt: string) =>
          completeWithFetch({
            apiKey: config.memberModelApiKey!,
            url: config.memberModelUrl!,
            prompt,
          })
      : undefined;

  async function ensurePeersInGroup(groupId: string, peers: string[]): Promise<void> {
    if (peers.length === 0) return;
    await agent.client.conversations.syncAll();
    const ctx = await agent.getConversationContext(groupId);
    if (!ctx || !ctx.isGroup()) {
      console.warn(`[member] group ${groupId} not found locally; is this member in the group?`);
      return;
    }
    const members = await ctx.conversation.members();
    const present = members.flatMap((m) => m.accountIdentifiers.map((i) => i.identifier));
    const missing = missingMembers(present, peers);
    if (missing.length === 0) return;
    await agent.addMembersWithAddresses(ctx.conversation, missing as `0x${string}`[]);
    console.log(`[member] added ${missing.length} peer(s) to group`);
  }

  agent.on("text", async (ctx: any) => {
    const sender = ((await ctx.getSenderAddress()) || "").toLowerCase();
    if (sender && sender === selfAddress) return;
    if (!isAllowedSender(sender, config.allowedSenders)) {
      await appendLedger(
        ledgerPath,
        buildLedgerEntry({
          now: new Date(),
          kind: "sender_dropped",
          peer: sender || null,
          reason: "sender_not_allowed",
        })
      );
      return;
    }

    const text =
      typeof ctx.message?.content === "string"
        ? ctx.message.content
        : ctx.message?.content?.text || "";

    try {
      loopState = await onInboundText({
        state: loopState,
        text,
        complete,
        approvedDays: config.approvedDays,
        sensitiveKeywords: config.sensitiveKeywords,
        recordOwner: async (note) => {
          await appendOwnerNote(ownerNotesPath, note);
        },
        sendText: async (body: string) => {
          await ctx.conversation.sendText(body);
        },
        saveState: async (nextState: LocalLoopState) => {
          await saveLoopState(statePath, nextState);
        },
      });
    } catch (err: any) {
      console.error("Worker process error:", err.message);
    }
  });

  const hubConfigured = Boolean(config.hubBaseUrl && config.hubRoomId && config.hubToken);
  const hubGroupId = hubConfigured
    ? await fetchHubGroupId({
        hubBaseUrl: config.hubBaseUrl!,
        hubRoomId: config.hubRoomId!,
        hubToken: config.hubToken!,
      })
    : null;

  const plan = resolveGroupPlan({
    envGroupId: config.xmtpGroupId,
    hubGroupId,
    peerAddresses: config.xmtpPeerAddresses || [],
  });

  if (plan.action === "create") {
    const group = await agent.createGroupWithAddresses(
      (config.xmtpPeerAddresses || []) as `0x${string}`[]
    );
    console.log(`[member] created group ${group.id}`);
    if (hubConfigured) {
      await bindGroupToHub(config.hubBaseUrl!, config.hubRoomId!, config.hubToken!, group.id);
    }
  } else if (plan.action === "use") {
    console.log(`[member] using group ${plan.groupId} from ${plan.source}`);
    await ensurePeersInGroup(plan.groupId, config.xmtpPeerAddresses || []);
  } else {
    console.log("[member] no group configured; listening only");
  }

  console.log(`[member] address ${selfAddress}`);
  await agent.start();
}

if (process.argv[1] && process.argv[1].endsWith("index.ts")) {
  startMember().catch((err) => {
    console.error("Worker process error:", err.message);
    process.exit(1);
  });
}
```

和旧版相比，改了这几处，请逐条核对：

1. 新增 `XMTP_GROUP_ID` 与 `XMTP_ALLOWED_SENDERS` 两个配置。
2. 名单为空时 `startMember` 直接抛错，不启动。
3. 收到消息先取发送者地址；是自己就忽略；不在名单里就写账本并返回，不进入模型。
4. 回复统一走 `ctx.conversation.sendText`。
5. 建群前先问 Hub；只有 `resolveGroupPlan` 返回 `create` 才新建。
6. 复用已有群时，调用 `ensurePeersInGroup`，把 `XMTP_PEER_ADDRESSES` 里还不在群里的地址加进去；已在群里的不会重复添加。
7. 主人记录和账本都放在 `XMTP_LOOP_STATE_PATH` 所在目录。

这份代码已在 XMTP `dev` 网络上实测：成员复用已有群时只补加缺少的地址；不在白名单里的发送者，其消息只进账本 `sender_dropped`。

- [ ] **Step 3: 更新 `.env.example`**

把 `workers/xmtp-member/.env.example` 整个替换为：

```text
XMTP_WALLET_KEY=
XMTP_DB_ENCRYPTION_KEY=
XMTP_ENV=dev
XMTP_DB_DIRECTORY=
XMTP_PEER_ADDRESSES=
XMTP_GROUP_ID=
XMTP_ALLOWED_SENDERS=
XMTP_SELF_NAME=
XMTP_LOOP_STATE_PATH=
HUB_BASE_URL=
HUB_TOKEN=
HUB_ROOM_ID=
MEMBER_MODEL_API_KEY=
MEMBER_MODEL_URL=
XMTP_APPROVED_DAYS=
XMTP_SENSITIVE_KEYWORDS=
OWNER_NOTES_PORT=
```

- [ ] **Step 4: 类型检查和全部测试**

```bash
cd workers/xmtp-member && npm run typecheck && npm test
```

Expected: `typecheck` 没有任何输出；测试 `ℹ pass 39`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/index.ts workers/xmtp-member/package.json workers/xmtp-member/.env.example
git commit -m "$(cat <<'EOF'
[XMTP] 成员进程只处理白名单发送者并复用已绑定的群

- Changed: 未配置 XMTP_ALLOWED_SENDERS 拒绝启动；非名单消息只进本机账本
- Changed: 启动时先读 Hub 房间的 xmtp_group_id，已有就不新建
- Added: npm run typecheck
- Handoff:
 - 依赖变更：无
 - 前后端联动：成员进程的主人需要把浏览器页面显示的"我的加密地址"填进 XMTP_ALLOWED_SENDERS（Task 7）
EOF
)"
```

---

---

### 等待点：A10 乙的阶段 A 代码已推送

```bash
git pull --rebase origin feat/owner-gateway
git log --oneline | rg "加密群只允许房主绑定且不可覆盖"
git log --oneline | rg "XMTP 网络可配置并显示本机加密地址"
```

两条都有输出才继续做 Task 10。缺哪条就停下等乙。

---

> 分工说明：本任务两人一起做。你做所有写着"甲机"的步骤；Step 6 的记录和提交由乙完成，你不要改 `ACCEPTANCE_CHECKLIST.md`。

### Task 10: 双机演练（人类配合）

**Files:**
- Modify: `ACCEPTANCE_CHECKLIST.md`（`Dual-machine drill` 行）

需要两台电脑，下面称"甲机"和"乙机"。每台都要有 Node 22 和本仓库。

- [ ] **Step 1: 两台机器各自生成成员进程密钥**

每台各执行两次，第一次的输出作为 `XMTP_WALLET_KEY`，第二次的输出作为 `XMTP_DB_ENCRYPTION_KEY`：

```bash
node -e "console.log('0x' + require('crypto').randomBytes(32).toString('hex'))"
```

算出成员进程地址：

```bash
cd workers/xmtp-member && node --input-type=module -e "import { privateKeyToAccount } from 'viem/accounts'; console.log(privateKeyToAccount(process.argv[1]).address.toLowerCase())" 0x<上一步的钱包私钥>
```

- [ ] **Step 2: 甲机房主在浏览器建加密房间**

1. 甲机前端的 `frontend/.env.local` 里把 `VITE_XMTP_WORKER_ADDRESS` 设为甲机成员进程地址，启动前端。
2. 乙机用户先在 Hub 注册账号。
3. 甲机登录，新建房间，在成员面板邀请乙为 member。先不要进房间。

Step 3 写环境文件，Step 4 按固定顺序启动。浏览器建群和拉人的顺序写在 Step 4 里。

- [ ] **Step 3: 两台机器各写一份 `workers/xmtp-member/.env`（不提交，`.env` 已在 `.gitignore`）**

甲机：

```text
XMTP_WALLET_KEY=<甲机钱包私钥>
XMTP_DB_ENCRYPTION_KEY=<甲机库密钥>
XMTP_ENV=dev
XMTP_DB_DIRECTORY=.state/xmtp-db
XMTP_ALLOWED_SENDERS=<甲浏览器地址>,<乙浏览器地址>,<乙成员进程地址>
XMTP_PEER_ADDRESSES=<乙浏览器地址>,<乙成员进程地址>
HUB_BASE_URL=<Hub 地址>
HUB_TOKEN=<甲机房主登录令牌>
HUB_ROOM_ID=<房间 ID>
OWNER_NOTES_PORT=8787
```

乙机：

```text
XMTP_WALLET_KEY=<乙机钱包私钥>
XMTP_DB_ENCRYPTION_KEY=<乙机库密钥>
XMTP_ENV=dev
XMTP_DB_DIRECTORY=.state/xmtp-db
XMTP_ALLOWED_SENDERS=<甲浏览器地址>,<乙浏览器地址>,<甲成员进程地址>
HUB_BASE_URL=<Hub 地址>
HUB_TOKEN=<乙登录令牌>
HUB_ROOM_ID=<房间 ID>
OWNER_NOTES_PORT=8787
```

乙机的 `XMTP_PEER_ADDRESSES` 留空，由甲机负责拉人。

- [ ] **Step 4: 按顺序启动**

启动命令（两台机器都一样）：

```bash
cd workers/xmtp-member && node --env-file=.env --experimental-strip-types src/index.ts
```

严格按下面顺序做：

1. 甲机：`.env` 里 `XMTP_PEER_ADDRESSES` 先留空，`XMTP_ALLOWED_SENDERS` 先只填甲浏览器地址（不知道就先填甲成员进程地址，名单不能为空）。启动。Expected：`[member] no group configured; listening only`。
2. 甲机浏览器进入房间。浏览器会建群、把甲机成员进程拉进群、绑定到 Hub。复制顶部"我的加密地址"。
3. 乙机浏览器进入同一房间，复制顶部"我的加密地址"，发给甲机主人。
4. 乙机：按 Step 3 写好 `.env`，启动。Expected：`[member] using group <群 id> from hub`，可能紧跟 `group ... not found locally`，这是正常的，因为还没被拉进群。
5. 甲机：按 Step 3 把 `XMTP_ALLOWED_SENDERS` 和 `XMTP_PEER_ADDRESSES` 填完整，按 Ctrl+C 停掉再启动。Expected：`[member] using group <同一个群 id> from hub` 和 `[member] added 2 peer(s) to group`。
6. 乙机：Ctrl+C 停掉再启动一次。Expected：`[member] using group <同一个群 id> from hub`，不再有 `not found locally`。
7. 乙机浏览器刷新页面。

两台机器的日志里都不能出现 `created group`。出现了就说明群被重复创建，停止并报告人类。

- [ ] **Step 5: 各发一条消息并检查**

甲、乙浏览器各发一句 `@Codex 双机演练`。检查三件事：

1. 两台浏览器都看到对方的消息。
2. 两台机器的 `http://127.0.0.1:8787/owner-notes` 各有新记录。
3. 甲机把 `XMTP_ALLOWED_SENDERS` 里的乙浏览器地址删掉，重启甲机成员进程；乙浏览器再发一句；甲机 `workers/xmtp-member/.state/ledger.jsonl` 出现 `"kind":"sender_dropped"`。然后把地址加回去，再重启。

- [ ] **Step 6: 记录并提交**

在 `ACCEPTANCE_CHECKLIST.md` 的 `Dual-machine drill` 行把 `Pending` 改为 `Passed 2026-MM-DD`，备注写"两机同群，非名单发送者被丢弃"。不要贴地址、密钥、群 id。

```bash
git add ACCEPTANCE_CHECKLIST.md
git commit -m "$(cat <<'EOF'
[文档] 记录双机演练结果

- Changed: Dual-machine drill 改为 Passed
- Handoff: 阶段 A 完成，可以进入阶段 B
EOF
)"
```

如果任一检查失败，不要改成 Passed，把失败现象写进备注，停止并报告人类。

---

---

## 5. 阶段 B：你的任务

以下先是原阶段 B 计划的开头部分，再是你的任务。

# 阶段 B：主人网关与单次同意 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `workers/xmtp-member` 升级成主人网关：别的成员向你申请某段日期的忙闲时间，你在本机控制台点"允许一次"后，对方只收到忙碌时段的开始和结束，拿不到日程标题、备注、参与人；越界申请自动拒绝，不打扰你。

**Architecture:** 群里的结构化消息是以 `MAPR1 ` 开头的一行 JSON（请求、等待批准、拒绝、披露四种）。被请求方网关按本机 `policy.json` 判断范围；在范围内就进同意队列，由主人在 `http://127.0.0.1:8787/` 批准。批准后网关用 XMTP 身份私钥签一张单次授权 `Grant`，把它和只含时段的数据一起发出。接收方核对签名、接收者、过期时间、数据哈希，全部通过才存进本机收件箱。Hub 不参与，读不到任何内容。

**Tech Stack:** Node 22、`@xmtp/agent-sdk` 2.3.0、`viem` 2.56.8（签名与验签）、`node --test`；前端 React + Vitest。

**本计划的每段代码都已在 2026-09-23 的临时副本里跑过：成员进程 85 项测试通过，类型检查通过；前端 22 项测试通过，构建通过；两个网关在 XMTP `dev` 网络上走完了"请求、等待批准、允许一次、收到只含时段的数据"和"越界自动拒绝"。照抄即可。**

---

## 开始前必读

1. 阶段 A 计划必须已经做完 Task 2 到 Task 8（Task 9、10 可以因为缺人类配合而未完成，但要在 `ACCEPTANCE_CHECKLIST.md` 写明）。检查方法：

```bash
git log --oneline feat/owner-gateway | head -20
cd workers/xmtp-member && npm run typecheck && npm test
```

Expected：能看到阶段 A 的提交；`ℹ pass 39`。不满足就停止，回去做阶段 A。

2. 本计划 Task 1 是人类审批关卡。
3. 所有命令从仓库根目录开始。

**不要做：**

- 不改 Hub 后端（`backend/`）。本阶段 Hub 什么都不用知道。
- 不接真实日历、邮箱。日历数据只来自本机 `calendar.json` 文件。
- 不让模型看到日历内容。`composeReply` 不改。
- 不把 `.state/` 下的任何文件、私钥、地址提交进 git。
- 不合并 `main`。

---

---

### 等待点：B0 阶段 B 已获批准

```bash
git pull --rebase origin feat/owner-gateway
git log --oneline | rg "记录阶段 B 架构批准"
```

有输出才开始阶段 B。没有就停下等乙。

阶段 B Task 1、9、10、11、13、15 由开发者乙执行，本文不包含。

---

### Task 2: 显式依赖 viem

**Files:**
- Modify: `workers/xmtp-member/package.json`
- Modify: `workers/xmtp-member/package-lock.json`（由 npm 生成）

- [ ] **Step 1: 安装**

```bash
cd workers/xmtp-member && npm install --save-exact viem@2.56.8
```

Expected：`package.json` 的 `dependencies` 里出现 `"viem": "2.56.8"`。

- [ ] **Step 2: 确认没打坏**

```bash
cd workers/xmtp-member && npm run typecheck && npm test
```

Expected：`ℹ pass 39`。

> 分工说明：如果乙已经推送了阶段 B Task 10（`localGuard`），这里会是 `ℹ pass 43`，也算正确。只要 `ℹ fail 0` 就继续。

- [ ] **Step 3: 提交**

```bash
git add workers/xmtp-member/package.json workers/xmtp-member/package-lock.json
git commit -m "$(cat <<'EOF'
[XMTP] 显式依赖 viem

- Added: viem 2.56.8
- Handoff:
 - 依赖变更：workers/xmtp-member 新增直接依赖 viem，用于授权签名与验签
EOF
)"
```

---

---

### Task 3: 结构化消息的编码与解码

**Files:**
- Create: `workers/xmtp-member/src/envelope.ts`
- Create: `workers/xmtp-member/src/envelope.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/envelope.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { decodeEnvelope, encodeEnvelope, ENVELOPE_PREFIX, type RequestEnvelope } from "./envelope.ts";

const TO = "0x70997970c51812dc3a010c7d01b50e0d17dc79c8";

const request: RequestEnvelope = {
  kind: "request",
  request_id: "req-1",
  to: TO,
  scope: "calendar.free_busy",
  purpose: "约下周的会",
  constraints: { date_from: "2026-10-05", date_to: "2026-10-09" },
};

test("request round-trips through encode and decode", () => {
  const text = encodeEnvelope(request);
  assert.equal(text.startsWith(ENVELOPE_PREFIX), true);
  assert.deepEqual(decodeEnvelope(text), request);
});

test("plain chat text is not an envelope", () => {
  assert.equal(decodeEnvelope("@Codex 你好"), null);
  assert.equal(decodeEnvelope("MAPR1 not json"), null);
});

test("uppercase or malformed recipient is rejected", () => {
  const bad = encodeEnvelope({ ...request, to: TO.toUpperCase().replace("0X", "0x") });
  assert.equal(decodeEnvelope(bad), null);
});

test("bad date range is rejected", () => {
  const bad = encodeEnvelope({ ...request, constraints: { date_from: "tomorrow", date_to: "2026-10-09" } });
  assert.equal(decodeEnvelope(bad), null);
});

test("unknown kind and unknown denial reason are rejected", () => {
  assert.equal(decodeEnvelope(`${ENVELOPE_PREFIX}{"kind":"exec","request_id":"r","to":"${TO}"}`), null);
  assert.equal(
    decodeEnvelope(`${ENVELOPE_PREFIX}{"kind":"denial","request_id":"r","to":"${TO}","reason":"because"}`),
    null
  );
});

test("extra fields on a request are dropped", () => {
  const text = `${ENVELOPE_PREFIX}${JSON.stringify({ ...request, run: "rm -rf /" })}`;
  const decoded = decodeEnvelope(text) as any;
  assert.equal(decoded.run, undefined);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/envelope.test.ts
```

Expected: FAIL，找不到 `envelope.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/envelope.ts`：

```ts
export const ENVELOPE_PREFIX = "MAPR1 ";

export const KNOWN_SCOPES = ["calendar.free_busy"] as const;

export interface DateRange {
  date_from: string;
  date_to: string;
}

export interface BusySlot {
  start: string;
  end: string;
}

export interface FreeBusyPayload {
  scope: "calendar.free_busy";
  date_from: string;
  date_to: string;
  busy: BusySlot[];
}

export interface Grant {
  grant_id: string;
  owner: string;
  audience: string;
  request_id: string;
  scope: string;
  constraints: DateRange;
  payload_hash: string;
  expires_at: string;
  max_uses: 1;
}

export interface RequestEnvelope {
  kind: "request";
  request_id: string;
  to: string;
  scope: string;
  purpose: string;
  constraints: DateRange;
}

export interface PendingEnvelope {
  kind: "consent_pending";
  request_id: string;
  to: string;
}

export type DenialReason = "out_of_scope" | "owner_denied" | "expired";

export interface DenialEnvelope {
  kind: "denial";
  request_id: string;
  to: string;
  reason: DenialReason;
}

export interface DisclosureEnvelope {
  kind: "disclosure";
  request_id: string;
  to: string;
  grant: Grant;
  signature: string;
  payload: FreeBusyPayload;
}

export type Envelope =
  | RequestEnvelope
  | PendingEnvelope
  | DenialEnvelope
  | DisclosureEnvelope;

const DATE_RE = /^\d{4}-\d{2}-\d{2}$/;
const ADDRESS_RE = /^0x[0-9a-f]{40}$/;
const DENIAL_REASONS: DenialReason[] = ["out_of_scope", "owner_denied", "expired"];

export function isAddress(value: unknown): value is string {
  return typeof value === "string" && ADDRESS_RE.test(value);
}

function isShortString(value: unknown, max: number): value is string {
  return typeof value === "string" && value.length > 0 && value.length <= max;
}

export function isDateRange(value: any): value is DateRange {
  return (
    !!value &&
    typeof value === "object" &&
    typeof value.date_from === "string" &&
    DATE_RE.test(value.date_from) &&
    typeof value.date_to === "string" &&
    DATE_RE.test(value.date_to)
  );
}

export function encodeEnvelope(envelope: Envelope): string {
  return ENVELOPE_PREFIX + JSON.stringify(envelope);
}

export function decodeEnvelope(text: string): Envelope | null {
  if (typeof text !== "string" || !text.startsWith(ENVELOPE_PREFIX)) return null;
  if (text.length > 64 * 1024) return null;
  let raw: any;
  try {
    raw = JSON.parse(text.slice(ENVELOPE_PREFIX.length));
  } catch {
    return null;
  }
  if (!raw || typeof raw !== "object") return null;
  if (!isShortString(raw.request_id, 64) || !isAddress(raw.to)) return null;

  switch (raw.kind) {
    case "request":
      if (!isShortString(raw.scope, 64)) return null;
      if (typeof raw.purpose !== "string" || raw.purpose.length > 280) return null;
      if (!isDateRange(raw.constraints)) return null;
      return {
        kind: "request",
        request_id: raw.request_id,
        to: raw.to,
        scope: raw.scope,
        purpose: raw.purpose,
        constraints: {
          date_from: raw.constraints.date_from,
          date_to: raw.constraints.date_to,
        },
      };
    case "consent_pending":
      return { kind: "consent_pending", request_id: raw.request_id, to: raw.to };
    case "denial":
      if (!DENIAL_REASONS.includes(raw.reason)) return null;
      return { kind: "denial", request_id: raw.request_id, to: raw.to, reason: raw.reason };
    case "disclosure":
      if (!raw.grant || typeof raw.grant !== "object") return null;
      if (!isShortString(raw.signature, 200)) return null;
      if (!raw.payload || typeof raw.payload !== "object") return null;
      return {
        kind: "disclosure",
        request_id: raw.request_id,
        to: raw.to,
        grant: raw.grant,
        signature: raw.signature,
        payload: raw.payload,
      };
    default:
      return null;
  }
}
```

要点（不用改代码，理解即可）：地址一律小写；解码时只拷贝已知字段，多余字段被丢掉；解码失败返回 `null`，调用方据此当作普通聊天或直接忽略。

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/envelope.test.ts
```

Expected: `ℹ pass 6`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/envelope.ts workers/xmtp-member/src/envelope.test.ts
git commit -m "$(cat <<'EOF'
[网关] 群内结构化消息 MAPR1 的编解码

- Added: encodeEnvelope、decodeEnvelope 与四种消息类型
- Handoff:
 - 接口变动：群消息协议新增 MAPR1 前缀，当前只支持 calendar.free_busy 一种范围
EOF
)"
```

---

> 做完本任务立刻推送，乙在等 `envelope.ts`。

```bash
git pull --rebase origin feat/owner-gateway && git push origin feat/owner-gateway
```

---

### Task 4: 单次授权的签发与核验

**Files:**
- Create: `workers/xmtp-member/src/grant.ts`
- Create: `workers/xmtp-member/src/grant.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/grant.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { buildGrant, signGrant, verifyGrant } from "./grant.ts";

// Hardhat 公开测试账号，只用于单元测试，不能用于任何真实网络。
const OWNER_KEY = "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80";
const OWNER = "0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266";
const OTHER_KEY = "0x59c6995e998f97a5a0044966f0945389dc9e86dae88c7a8412f4603b6b78690d";
const AUDIENCE = "0x70997970c51812dc3a010c7d01b50e0d17dc79c8";
const NOW = new Date("2026-10-01T08:00:00Z");
const payload = {
  scope: "calendar.free_busy" as const,
  date_from: "2026-10-05",
  date_to: "2026-10-09",
  busy: [{ start: "2026-10-05T09:00:00.000Z", end: "2026-10-05T10:00:00.000Z" }],
};

function makeGrant() {
  return buildGrant({
    owner: OWNER,
    audience: AUDIENCE,
    requestId: "req-1",
    scope: "calendar.free_busy",
    constraints: { date_from: "2026-10-05", date_to: "2026-10-09" },
    payload,
    now: NOW,
  });
}

test("a signed grant verifies for the right owner, audience and payload", async () => {
  const grant = makeGrant();
  const signature = await signGrant(grant, OWNER_KEY);
  const result = await verifyGrant({
    grant, signature, expectedOwner: OWNER, expectedAudience: AUDIENCE, payload, now: NOW,
  });
  assert.deepEqual(result, { ok: true });
});

test("signing with a key that is not the owner throws", async () => {
  await assert.rejects(signGrant(makeGrant(), OTHER_KEY), /does not match/);
});

test("a tampered payload is rejected", async () => {
  const grant = makeGrant();
  const signature = await signGrant(grant, OWNER_KEY);
  const tampered = { ...payload, busy: [] };
  const result = await verifyGrant({
    grant, signature, expectedOwner: OWNER, expectedAudience: AUDIENCE, payload: tampered, now: NOW,
  });
  assert.deepEqual(result, { ok: false, reason: "payload_mismatch" });
});

test("a grant sent by someone other than its owner is rejected", async () => {
  const grant = makeGrant();
  const signature = await signGrant(grant, OWNER_KEY);
  const result = await verifyGrant({
    grant, signature, expectedOwner: AUDIENCE, expectedAudience: AUDIENCE, payload, now: NOW,
  });
  assert.deepEqual(result, { ok: false, reason: "wrong_owner" });
});

test("an edited grant field breaks the signature", async () => {
  const grant = makeGrant();
  const signature = await signGrant(grant, OWNER_KEY);
  const edited = { ...grant, constraints: { date_from: "2026-10-01", date_to: "2026-10-30" } };
  const result = await verifyGrant({
    grant: edited, signature, expectedOwner: OWNER, expectedAudience: AUDIENCE, payload, now: NOW,
  });
  assert.deepEqual(result, { ok: false, reason: "bad_signature" });
});

test("an expired grant is rejected", async () => {
  const grant = makeGrant();
  const signature = await signGrant(grant, OWNER_KEY);
  const later = new Date(NOW.getTime() + 11 * 60 * 1000);
  const result = await verifyGrant({
    grant, signature, expectedOwner: OWNER, expectedAudience: AUDIENCE, payload, now: later,
  });
  assert.deepEqual(result, { ok: false, reason: "expired" });
});
```

测试里的两把私钥是 Hardhat 公开测试账号，全世界都知道，只能用在测试里，不许用于任何网络。

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/grant.test.ts
```

Expected: FAIL，找不到 `grant.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/grant.ts`：

```ts
import { createHash, randomUUID } from "node:crypto";
import { verifyMessage } from "viem";
import { privateKeyToAccount } from "viem/accounts";
import { isAddress, isDateRange, type DateRange, type Grant } from "./envelope.ts";

export const GRANT_TTL_MS = 10 * 60 * 1000;

export function hashPayload(payload: unknown): string {
  return createHash("sha256").update(JSON.stringify(payload)).digest("hex");
}

export function buildGrant(input: {
  owner: string;
  audience: string;
  requestId: string;
  scope: string;
  constraints: DateRange;
  payload: unknown;
  now: Date;
}): Grant {
  return {
    grant_id: randomUUID(),
    owner: input.owner.toLowerCase(),
    audience: input.audience.toLowerCase(),
    request_id: input.requestId,
    scope: input.scope,
    constraints: {
      date_from: input.constraints.date_from,
      date_to: input.constraints.date_to,
    },
    payload_hash: hashPayload(input.payload),
    expires_at: new Date(input.now.getTime() + GRANT_TTL_MS).toISOString(),
    max_uses: 1,
  };
}

export function grantMessage(grant: Grant): string {
  return (
    "MAPR1-GRANT " +
    JSON.stringify([
      grant.grant_id,
      grant.owner,
      grant.audience,
      grant.request_id,
      grant.scope,
      grant.constraints.date_from,
      grant.constraints.date_to,
      grant.payload_hash,
      grant.expires_at,
      grant.max_uses,
    ])
  );
}

export async function signGrant(grant: Grant, privateKey: string): Promise<string> {
  const account = privateKeyToAccount(privateKey as `0x${string}`);
  if (account.address.toLowerCase() !== grant.owner) {
    throw new Error("grant owner does not match signing key");
  }
  return account.signMessage({ message: grantMessage(grant) });
}

export function isGrant(value: any): value is Grant {
  return (
    !!value &&
    typeof value === "object" &&
    typeof value.grant_id === "string" &&
    isAddress(value.owner) &&
    isAddress(value.audience) &&
    typeof value.request_id === "string" &&
    typeof value.scope === "string" &&
    isDateRange(value.constraints) &&
    typeof value.payload_hash === "string" &&
    typeof value.expires_at === "string" &&
    value.max_uses === 1
  );
}

export type VerifyResult =
  | { ok: true }
  | {
      ok: false;
      reason:
        | "malformed"
        | "wrong_owner"
        | "wrong_audience"
        | "expired"
        | "payload_mismatch"
        | "bad_signature";
    };

export async function verifyGrant(input: {
  grant: unknown;
  signature: string;
  expectedOwner: string;
  expectedAudience: string;
  payload: unknown;
  now: Date;
}): Promise<VerifyResult> {
  if (!isGrant(input.grant)) return { ok: false, reason: "malformed" };
  const grant = input.grant;
  if (grant.owner !== input.expectedOwner.toLowerCase()) return { ok: false, reason: "wrong_owner" };
  if (grant.audience !== input.expectedAudience.toLowerCase()) {
    return { ok: false, reason: "wrong_audience" };
  }
  if (Date.parse(grant.expires_at) <= input.now.getTime()) return { ok: false, reason: "expired" };
  if (grant.payload_hash !== hashPayload(input.payload)) {
    return { ok: false, reason: "payload_mismatch" };
  }
  let valid = false;
  try {
    valid = await verifyMessage({
      address: grant.owner as `0x${string}`,
      message: grantMessage(grant),
      signature: input.signature as `0x${string}`,
    });
  } catch {
    valid = false;
  }
  return valid ? { ok: true } : { ok: false, reason: "bad_signature" };
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/grant.test.ts
```

Expected: `ℹ pass 6`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/grant.ts workers/xmtp-member/src/grant.test.ts
git commit -m "$(cat <<'EOF'
[网关] 单次授权的签发与核验

- Added: buildGrant、signGrant、verifyGrant、hashPayload
- Handoff: 授权 10 分钟过期；核验顺序为结构、签发人、接收人、过期、数据哈希、签名
EOF
)"
```

---

---

### Task 5: 虚构日历连接器（只输出忙碌时段）

**Files:**
- Create: `workers/xmtp-member/src/calendar.ts`
- Create: `workers/xmtp-member/src/calendar.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/calendar.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { freeBusy, loadCalendar, type CalendarEvent } from "./calendar.ts";

const events: CalendarEvent[] = [
  {
    start: "2026-10-06T09:00:00Z",
    end: "2026-10-06T10:00:00Z",
    title: "和律师谈离婚协议",
    notes: "带上房产证",
    attendees: ["lawyer@example.com"],
  },
  { start: "2026-10-05T14:00:00Z", end: "2026-10-05T15:00:00Z", title: "体检" },
  { start: "2026-10-20T09:00:00Z", end: "2026-10-20T10:00:00Z", title: "范围外" },
];

test("free/busy only contains start and end, sorted, inside the range", () => {
  const payload = freeBusy(events, { date_from: "2026-10-05", date_to: "2026-10-09" });
  assert.deepEqual(payload, {
    scope: "calendar.free_busy",
    date_from: "2026-10-05",
    date_to: "2026-10-09",
    busy: [
      { start: "2026-10-05T14:00:00.000Z", end: "2026-10-05T15:00:00.000Z" },
      { start: "2026-10-06T09:00:00.000Z", end: "2026-10-06T10:00:00.000Z" },
    ],
  });
  const encoded = JSON.stringify(payload);
  for (const secret of ["律师", "房产证", "lawyer@example.com", "体检", "范围外"]) {
    assert.equal(encoded.includes(secret), false, secret);
  }
});

test("the last day of the range is included", () => {
  const payload = freeBusy(
    [{ start: "2026-10-09T23:00:00Z", end: "2026-10-09T23:30:00Z" }],
    { date_from: "2026-10-05", date_to: "2026-10-09" }
  );
  assert.equal(payload.busy.length, 1);
});

test("loadCalendar returns [] for a missing file and filters bad rows", async () => {
  const dir = await mkdtemp(path.join(os.tmpdir(), "cal-"));
  assert.deepEqual(await loadCalendar(path.join(dir, "none.json")), []);
  const file = path.join(dir, "calendar.json");
  await writeFile(file, JSON.stringify([{ start: "2026-10-05T09:00:00Z", end: "2026-10-05T10:00:00Z" }, { title: "no time" }]));
  const loaded = await loadCalendar(file);
  assert.equal(loaded.length, 1);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/calendar.test.ts
```

Expected: FAIL，找不到 `calendar.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/calendar.ts`：

```ts
import { existsSync } from "node:fs";
import { readFile } from "node:fs/promises";
import type { DateRange, FreeBusyPayload } from "./envelope.ts";

export interface CalendarEvent {
  start: string;
  end: string;
  title?: string;
  notes?: string;
  attendees?: string[];
}

const DAY_MS = 24 * 60 * 60 * 1000;

export async function loadCalendar(file: string): Promise<CalendarEvent[]> {
  if (!existsSync(file)) return [];
  const raw = JSON.parse(await readFile(file, "utf8"));
  if (!Array.isArray(raw)) throw new Error("calendar file must be a JSON array");
  return raw.filter(
    (e: any) => e && typeof e.start === "string" && typeof e.end === "string"
  );
}

export function freeBusy(events: CalendarEvent[], range: DateRange): FreeBusyPayload {
  const from = Date.parse(`${range.date_from}T00:00:00Z`);
  const to = Date.parse(`${range.date_to}T00:00:00Z`) + DAY_MS;
  const busy = events
    .map((e) => ({ s: Date.parse(e.start), e: Date.parse(e.end) }))
    .filter((x) => Number.isFinite(x.s) && Number.isFinite(x.e) && x.s < to && x.e > from)
    .map((x) => ({ start: new Date(x.s).toISOString(), end: new Date(x.e).toISOString() }))
    .sort((a, b) => a.start.localeCompare(b.start));
  return {
    scope: "calendar.free_busy",
    date_from: range.date_from,
    date_to: range.date_to,
    busy,
  };
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/calendar.test.ts
```

Expected: `ℹ pass 3`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/calendar.ts workers/xmtp-member/src/calendar.test.ts
git commit -m "$(cat <<'EOF'
[网关] 本机虚构日历只输出忙碌时段

- Added: loadCalendar、freeBusy
- Handoff: 输出只含 start/end，标题、备注、参与人在函数内部就被丢掉
EOF
)"
```

---

---

### Task 6: 范围策略

**Files:**
- Create: `workers/xmtp-member/src/policy.ts`
- Create: `workers/xmtp-member/src/policy.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/policy.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp, writeFile } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { checkRequest, loadPolicy, normalizePolicy } from "./policy.ts";

const PEER = "0x70997970c51812dc3a010c7d01b50e0d17dc79c8";
const policy = normalizePolicy({
  allow: { [PEER.toUpperCase().replace("0X", "0x")]: ["calendar.free_busy"] },
  max_range_days: 7,
});
const week = { date_from: "2026-10-05", date_to: "2026-10-09" };

test("an allowed peer asking an allowed scope inside the limit passes", () => {
  assert.deepEqual(checkRequest(policy, PEER, { scope: "calendar.free_busy", constraints: week }), { ok: true });
});

test("a follow-up for attendees is out of scope", () => {
  assert.deepEqual(
    checkRequest(policy, PEER, { scope: "calendar.attendees", constraints: week }),
    { ok: false, reason: "out_of_scope" }
  );
});

test("an unknown peer is out of scope", () => {
  assert.deepEqual(
    checkRequest(policy, "0x1111111111111111111111111111111111111111", { scope: "calendar.free_busy", constraints: week }),
    { ok: false, reason: "out_of_scope" }
  );
});

test("a range longer than max_range_days or reversed is out of scope", () => {
  assert.equal(
    checkRequest(policy, PEER, { scope: "calendar.free_busy", constraints: { date_from: "2026-10-01", date_to: "2026-10-30" } }).ok,
    false
  );
  assert.equal(
    checkRequest(policy, PEER, { scope: "calendar.free_busy", constraints: { date_from: "2026-10-09", date_to: "2026-10-05" } }).ok,
    false
  );
});

test("missing policy file means nobody may ask anything", async () => {
  const dir = await mkdtemp(path.join(os.tmpdir(), "policy-"));
  const loaded = await loadPolicy(path.join(dir, "policy.json"));
  assert.deepEqual(loaded.allow, {});
  const file = path.join(dir, "policy2.json");
  await writeFile(file, JSON.stringify({ allow: { [PEER]: ["calendar.free_busy"] }, max_range_days: 99 }));
  const loaded2 = await loadPolicy(file);
  assert.equal(loaded2.max_range_days, 14);
  assert.deepEqual(loaded2.allow[PEER], ["calendar.free_busy"]);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/policy.test.ts
```

Expected: FAIL，找不到 `policy.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/policy.ts`：

```ts
import { existsSync } from "node:fs";
import { readFile } from "node:fs/promises";
import { KNOWN_SCOPES, type DateRange } from "./envelope.ts";

export interface Policy {
  allow: Record<string, string[]>;
  max_range_days: number;
}

const DAY_MS = 24 * 60 * 60 * 1000;

export function defaultPolicy(): Policy {
  return { allow: {}, max_range_days: 14 };
}

export function normalizePolicy(raw: any): Policy {
  const allow: Record<string, string[]> = {};
  if (raw && typeof raw.allow === "object" && raw.allow !== null) {
    for (const [address, scopes] of Object.entries(raw.allow)) {
      if (Array.isArray(scopes)) {
        allow[address.toLowerCase()] = scopes.filter((s): s is string => typeof s === "string");
      }
    }
  }
  const days = Number(raw?.max_range_days);
  const max_range_days = Number.isInteger(days) && days >= 1 && days <= 31 ? days : 14;
  return { allow, max_range_days };
}

export async function loadPolicy(file: string): Promise<Policy> {
  if (!existsSync(file)) return defaultPolicy();
  return normalizePolicy(JSON.parse(await readFile(file, "utf8")));
}

export function checkRequest(
  policy: Policy,
  sender: string,
  request: { scope: string; constraints: DateRange }
): { ok: true } | { ok: false; reason: "out_of_scope" } {
  const denied = { ok: false as const, reason: "out_of_scope" as const };
  const known = (KNOWN_SCOPES as readonly string[]).includes(request.scope);
  const allowed = policy.allow[sender.toLowerCase()] ?? [];
  if (!known || !allowed.includes(request.scope)) return denied;
  const from = Date.parse(`${request.constraints.date_from}T00:00:00Z`);
  const to = Date.parse(`${request.constraints.date_to}T00:00:00Z`);
  if (!Number.isFinite(from) || !Number.isFinite(to) || to < from) return denied;
  const days = (to - from) / DAY_MS + 1;
  if (days > policy.max_range_days) return denied;
  return { ok: true };
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/policy.test.ts
```

Expected: `ℹ pass 5`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/policy.ts workers/xmtp-member/src/policy.test.ts
git commit -m "$(cat <<'EOF'
[网关] 按成员地址限定可申请的范围

- Added: loadPolicy、normalizePolicy、checkRequest
- Handoff: 没有 policy.json 时任何人都不能申请任何范围；日期跨度默认最多 14 天，配置上限 31 天
EOF
)"
```

---

---

### Task 7: 同意队列与收件箱

**Files:**
- Create: `workers/xmtp-member/src/consentQueue.ts`
- Create: `workers/xmtp-member/src/consentQueue.test.ts`
- Create: `workers/xmtp-member/src/inbox.ts`
- Create: `workers/xmtp-member/src/inbox.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/consentQueue.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { ConsentQueue } from "./consentQueue.ts";

async function freshQueue() {
  const dir = await mkdtemp(path.join(os.tmpdir(), "consents-"));
  return { file: path.join(dir, "consents.json"), queue: new ConsentQueue(path.join(dir, "consents.json")) };
}

const item = {
  request_id: "req-1",
  requester: "0x70997970c51812dc3a010c7d01b50e0d17dc79c8",
  conversation_id: "conv-1",
  scope: "calendar.free_busy",
  purpose: "约会",
  constraints: { date_from: "2026-10-05", date_to: "2026-10-09" },
  created_at: "2026-10-01T08:00:00.000Z",
};

test("add is idempotent per request_id and persists across reloads", async () => {
  const { file, queue } = await freshQueue();
  assert.equal(await queue.add(item), true);
  assert.equal(await queue.add(item), false);
  const reloaded = new ConsentQueue(file);
  await reloaded.load();
  assert.equal(reloaded.list().length, 1);
  assert.equal(reloaded.get("req-1")?.status, "pending");
});

test("claim works once; the second concurrent claim gets null", async () => {
  const { queue } = await freshQueue();
  await queue.add(item);
  const [a, b] = await Promise.all([queue.claim("req-1", "approved"), queue.claim("req-1", "denied")]);
  assert.equal(a?.status, "approved");
  assert.equal(b, null);
  assert.equal(queue.get("req-1")?.status, "approved");
});

test("dueForExpiry lists only old pending items", async () => {
  const { queue } = await freshQueue();
  await queue.add(item);
  await queue.add({ ...item, request_id: "req-2", created_at: "2026-10-01T08:09:00.000Z" });
  const due = queue.dueForExpiry(new Date("2026-10-01T08:10:00.000Z"), 10 * 60 * 1000);
  assert.deepEqual(due.map((d) => d.request_id), ["req-1"]);
});

test("list returns copies that cannot mutate the queue", async () => {
  const { queue } = await freshQueue();
  await queue.add(item);
  queue.list()[0].status = "approved";
  assert.equal(queue.get("req-1")?.status, "pending");
});
```

新建 `workers/xmtp-member/src/inbox.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { appendInboxOnce, readInbox } from "./inbox.ts";

test("the same grant is stored only once", async () => {
  const dir = await mkdtemp(path.join(os.tmpdir(), "inbox-"));
  const file = path.join(dir, "inbox.json");
  const entry = {
    received_at: "2026-10-01T08:00:00.000Z",
    from: "0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266",
    request_id: "req-1",
    grant_id: "g-1",
    payload: { scope: "calendar.free_busy" as const, date_from: "2026-10-05", date_to: "2026-10-09", busy: [] },
  };
  assert.equal(await appendInboxOnce(file, entry), true);
  assert.equal(await appendInboxOnce(file, entry), false);
  assert.equal((await readInbox(file)).length, 1);
});
```

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/consentQueue.test.ts src/inbox.test.ts
```

Expected: FAIL，找不到两个模块。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/consentQueue.ts`：

```ts
import { existsSync } from "node:fs";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import type { DateRange } from "./envelope.ts";

export type ConsentStatus = "pending" | "approved" | "denied" | "expired";

export interface PendingConsent {
  request_id: string;
  requester: string;
  conversation_id: string;
  scope: string;
  purpose: string;
  constraints: DateRange;
  created_at: string;
  status: ConsentStatus;
}

function copy(item: PendingConsent): PendingConsent {
  return { ...item, constraints: { ...item.constraints } };
}

export class ConsentQueue {
  private items: PendingConsent[] = [];
  private readonly file: string;

  constructor(file: string) {
    this.file = file;
  }

  async load(): Promise<void> {
    if (!existsSync(this.file)) return;
    const raw = JSON.parse(await readFile(this.file, "utf8"));
    this.items = Array.isArray(raw) ? raw : [];
  }

  list(): PendingConsent[] {
    return this.items.map(copy);
  }

  get(requestId: string): PendingConsent | null {
    const found = this.items.find((i) => i.request_id === requestId);
    return found ? copy(found) : null;
  }

  async add(item: Omit<PendingConsent, "status">): Promise<boolean> {
    if (this.items.some((i) => i.request_id === item.request_id)) return false;
    this.items.push({ ...item, constraints: { ...item.constraints }, status: "pending" });
    await this.save();
    return true;
  }

  // Status changes synchronously before the first await, so two concurrent
  // callers can never both claim the same pending item.
  async claim(
    requestId: string,
    status: Exclude<ConsentStatus, "pending">
  ): Promise<PendingConsent | null> {
    const found = this.items.find((i) => i.request_id === requestId);
    if (!found || found.status !== "pending") return null;
    found.status = status;
    await this.save();
    return copy(found);
  }

  dueForExpiry(now: Date, ttlMs: number): PendingConsent[] {
    return this.items
      .filter((i) => i.status === "pending" && Date.parse(i.created_at) + ttlMs <= now.getTime())
      .map(copy);
  }

  private async save(): Promise<void> {
    await mkdir(path.dirname(this.file), { recursive: true });
    await writeFile(this.file, JSON.stringify(this.items, null, 2), "utf8");
  }
}
```

新建 `workers/xmtp-member/src/inbox.ts`：

```ts
import { existsSync } from "node:fs";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import path from "node:path";
import type { FreeBusyPayload } from "./envelope.ts";

export interface InboxEntry {
  received_at: string;
  from: string;
  request_id: string;
  grant_id: string;
  payload: FreeBusyPayload;
}

export async function readInbox(file: string): Promise<InboxEntry[]> {
  if (!existsSync(file)) return [];
  const raw = JSON.parse(await readFile(file, "utf8"));
  return Array.isArray(raw) ? raw : [];
}

export async function appendInboxOnce(file: string, entry: InboxEntry): Promise<boolean> {
  const all = await readInbox(file);
  if (all.some((e) => e.grant_id === entry.grant_id)) return false;
  all.push(entry);
  await mkdir(path.dirname(file), { recursive: true });
  await writeFile(file, JSON.stringify(all, null, 2), "utf8");
  return true;
}
```

`claim` 的注释说明了为什么它能防止双击批准：状态在第一个 `await` 之前就改掉了。不要把 `found.status = status;` 挪到 `await this.save()` 之后。

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/consentQueue.test.ts src/inbox.test.ts
```

Expected: `ℹ pass 5`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/consentQueue.ts workers/xmtp-member/src/consentQueue.test.ts workers/xmtp-member/src/inbox.ts workers/xmtp-member/src/inbox.test.ts
git commit -m "$(cat <<'EOF'
[网关] 同意队列与收件箱

- Added: ConsentQueue（add、claim、dueForExpiry）、appendInboxOnce、readInbox
- Handoff: 同一 request_id 只入队一次；同一 grant_id 只入收件箱一次
EOF
)"
```

---

> 做完本任务立刻推送，乙在等 `consentQueue.ts` 和 `inbox.ts`。

```bash
git pull --rebase origin feat/owner-gateway && git push origin feat/owner-gateway
```

---

### Task 8: 处理收到的结构化消息，以及主人的批准和拒绝

**Files:**
- Create: `workers/xmtp-member/src/handleEnvelope.ts`
- Create: `workers/xmtp-member/src/consentActions.ts`
- Create: `workers/xmtp-member/src/consentFlow.test.ts`

- [ ] **Step 1: 写失败的测试**

新建 `workers/xmtp-member/src/consentFlow.test.ts`：

```ts
import assert from "node:assert/strict";
import test from "node:test";
import { mkdtemp } from "node:fs/promises";
import os from "node:os";
import path from "node:path";
import { approveConsent, denyConsent, expireDue, type ConsentActionDeps } from "./consentActions.ts";
import { ConsentQueue } from "./consentQueue.ts";
import { decodeEnvelope, encodeEnvelope, type DisclosureEnvelope, type RequestEnvelope } from "./envelope.ts";
import { handleEnvelope, type HandleEnvelopeDeps } from "./handleEnvelope.ts";
import type { LedgerEntry } from "./ledger.ts";
import { normalizePolicy } from "./policy.ts";

// Hardhat 公开测试账号，只用于单元测试。
const B_KEY = "0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80";
const B = "0xf39fd6e51aad88f6f4ce6ab8827279cfffb92266"; // 被请求方（日历主人）
const A = "0x70997970c51812dc3a010c7d01b50e0d17dc79c8"; // 请求方
const NOW = new Date("2026-10-01T08:00:00Z");

const calendar = [
  { start: "2026-10-06T09:00:00Z", end: "2026-10-06T10:00:00Z", title: "和律师谈离婚协议", notes: "带上房产证" },
];

async function setup() {
  const dir = await mkdtemp(path.join(os.tmpdir(), "flow-"));
  const queue = new ConsentQueue(path.join(dir, "consents.json"));
  const sent: { conv: string; text: string }[] = [];
  const ledger: LedgerEntry[] = [];
  const stored: DisclosureEnvelope[] = [];
  const send = async (conv: string, text: string) => {
    sent.push({ conv, text });
  };
  const appendLedger = async (e: LedgerEntry) => {
    ledger.push(e);
  };
  const bDeps: HandleEnvelopeDeps = {
    selfAddress: B,
    policy: normalizePolicy({ allow: { [A]: ["calendar.free_busy"] } }),
    queue,
    send,
    appendLedger,
    storeDisclosure: async () => true,
    now: () => NOW,
  };
  const bActions: ConsentActionDeps = {
    selfAddress: B,
    privateKey: B_KEY,
    queue,
    loadCalendar: async () => calendar,
    send,
    appendLedger,
    now: () => NOW,
  };
  const aDeps: HandleEnvelopeDeps = {
    selfAddress: A,
    policy: normalizePolicy({}),
    queue: new ConsentQueue(path.join(dir, "a-consents.json")),
    send,
    appendLedger,
    storeDisclosure: async (d) => {
      stored.push(d);
      return true;
    },
    now: () => NOW,
  };
  return { queue, sent, ledger, stored, bDeps, bActions, aDeps };
}

const request: RequestEnvelope = {
  kind: "request",
  request_id: "req-1",
  to: B,
  scope: "calendar.free_busy",
  purpose: "约下周的会",
  constraints: { date_from: "2026-10-05", date_to: "2026-10-09" },
};

test("request not addressed to me is ignored and nothing is sent", async () => {
  const s = await setup();
  const outcome = await handleEnvelope({ envelope: { ...request, to: A }, sender: A, conversationId: "c1" }, s.bDeps);
  assert.equal(outcome, "ignored");
  assert.equal(s.sent.length, 0);
});

test("out-of-scope follow-up is denied automatically without asking the owner", async () => {
  const s = await setup();
  const outcome = await handleEnvelope(
    { envelope: { ...request, scope: "calendar.attendees" }, sender: A, conversationId: "c1" },
    s.bDeps
  );
  assert.equal(outcome, "denied");
  assert.equal(s.queue.list().length, 0);
  assert.deepEqual(decodeEnvelope(s.sent[0].text), { kind: "denial", request_id: "req-1", to: A, reason: "out_of_scope" });
});

test("allowed request is queued and the group is told it is pending", async () => {
  const s = await setup();
  const outcome = await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  assert.equal(outcome, "queued");
  assert.equal(s.queue.get("req-1")?.status, "pending");
  assert.equal(decodeEnvelope(s.sent[0].text)?.kind, "consent_pending");
  const again = await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  assert.equal(again, "duplicate");
});

test("approve sends only busy slots; the requester verifies and stores it", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  const result = await approveConsent("req-1", s.bActions);
  assert.deepEqual(result, { ok: true });
  const disclosureText = s.sent[s.sent.length - 1].text;
  for (const secret of ["律师", "房产证", "离婚"]) {
    assert.equal(disclosureText.includes(secret), false, secret);
  }
  const disclosure = decodeEnvelope(disclosureText)!;
  assert.equal(disclosure.kind, "disclosure");
  const outcome = await handleEnvelope({ envelope: disclosure, sender: B, conversationId: "c1" }, s.aDeps);
  assert.equal(outcome, "stored");
  assert.equal(s.stored.length, 1);
  assert.deepEqual(s.stored[0].payload.busy, [{ start: "2026-10-06T09:00:00.000Z", end: "2026-10-06T10:00:00.000Z" }]);
});

test("approving twice sends only one disclosure", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  const [r1, r2] = await Promise.all([approveConsent("req-1", s.bActions), approveConsent("req-1", s.bActions)]);
  assert.equal([r1.ok, r2.ok].filter(Boolean).length, 1);
  assert.equal(s.sent.filter((m) => decodeEnvelope(m.text)?.kind === "disclosure").length, 1);
});

test("a disclosure forwarded by a third party is rejected", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  await approveConsent("req-1", s.bActions);
  const disclosure = decodeEnvelope(s.sent[s.sent.length - 1].text)!;
  const outcome = await handleEnvelope(
    { envelope: disclosure, sender: "0x1111111111111111111111111111111111111111", conversationId: "c1" },
    s.aDeps
  );
  assert.equal(outcome, "rejected");
  assert.equal(s.stored.length, 0);
});

test("a disclosure with edited payload is rejected", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  await approveConsent("req-1", s.bActions);
  const disclosure = decodeEnvelope(s.sent[s.sent.length - 1].text) as DisclosureEnvelope;
  const edited = decodeEnvelope(encodeEnvelope({ ...disclosure, payload: { ...disclosure.payload, busy: [] } }))!;
  assert.equal(await handleEnvelope({ envelope: edited, sender: B, conversationId: "c1" }, s.aDeps), "rejected");
});

test("deny sends owner_denied and a later approve does nothing", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  assert.deepEqual(await denyConsent("req-1", "owner_denied", s.bActions), { ok: true });
  assert.deepEqual(decodeEnvelope(s.sent[s.sent.length - 1].text), {
    kind: "denial", request_id: "req-1", to: A, reason: "owner_denied",
  });
  assert.deepEqual(await approveConsent("req-1", s.bActions), { ok: false, reason: "not_pending" });
});

test("pending requests expire after ten minutes", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  const later = { ...s.bActions, now: () => new Date(NOW.getTime() + 10 * 60 * 1000) };
  assert.equal(await expireDue(later), 1);
  assert.equal(s.queue.get("req-1")?.status, "expired");
  assert.equal((decodeEnvelope(s.sent[s.sent.length - 1].text) as any).reason, "expired");
});

test("ledger never contains the purpose text or calendar titles", async () => {
  const s = await setup();
  await handleEnvelope({ envelope: request, sender: A, conversationId: "c1" }, s.bDeps);
  await approveConsent("req-1", s.bActions);
  const encoded = JSON.stringify(s.ledger);
  for (const secret of ["约下周的会", "律师", "房产证"]) {
    assert.equal(encoded.includes(secret), false, secret);
  }
});
```

这 10 项测试就是本阶段的验收标准，逐条对应：不是发给我的忽略；越界自动拒绝；在范围内入队并回"等待批准"；批准后只发时段且对方能验签入库；双击只发一次；第三方转发被拒；改过数据被拒；拒绝后不能再批准；10 分钟超时；账本里没有用途文字和日历标题。

- [ ] **Step 2: 运行，确认失败**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/consentFlow.test.ts
```

Expected: FAIL，找不到 `consentActions.ts` 或 `handleEnvelope.ts`。

- [ ] **Step 3: 写实现**

新建 `workers/xmtp-member/src/handleEnvelope.ts`：

```ts
import type { ConsentQueue } from "./consentQueue.ts";
import { encodeEnvelope, type DisclosureEnvelope, type Envelope } from "./envelope.ts";
import { verifyGrant } from "./grant.ts";
import { buildLedgerEntry, type LedgerEntry } from "./ledger.ts";
import { checkRequest, type Policy } from "./policy.ts";

export interface HandleEnvelopeDeps {
  selfAddress: string;
  policy: Policy;
  queue: ConsentQueue;
  send: (conversationId: string, text: string) => Promise<void>;
  appendLedger: (entry: LedgerEntry) => Promise<void>;
  storeDisclosure: (disclosure: DisclosureEnvelope, from: string) => Promise<boolean>;
  now: () => Date;
}

export type HandleOutcome =
  | "ignored"
  | "denied"
  | "queued"
  | "duplicate"
  | "stored"
  | "rejected"
  | "noted";

export async function handleEnvelope(
  input: { envelope: Envelope; sender: string; conversationId: string },
  deps: HandleEnvelopeDeps
): Promise<HandleOutcome> {
  const self = deps.selfAddress.toLowerCase();
  const sender = input.sender.toLowerCase();
  const env = input.envelope;
  if (env.to !== self) return "ignored";
  const now = deps.now();

  if (env.kind === "request") {
    const check = checkRequest(deps.policy, sender, env);
    if (!check.ok) {
      await deps.send(
        input.conversationId,
        encodeEnvelope({ kind: "denial", request_id: env.request_id, to: sender, reason: "out_of_scope" })
      );
      await deps.appendLedger(
        buildLedgerEntry({
          now, kind: "request_denied", requestId: env.request_id, peer: sender, scope: env.scope, reason: "out_of_scope",
        })
      );
      return "denied";
    }
    const added = await deps.queue.add({
      request_id: env.request_id,
      requester: sender,
      conversation_id: input.conversationId,
      scope: env.scope,
      purpose: env.purpose,
      constraints: env.constraints,
      created_at: now.toISOString(),
    });
    if (!added) return "duplicate";
    await deps.send(
      input.conversationId,
      encodeEnvelope({ kind: "consent_pending", request_id: env.request_id, to: sender })
    );
    await deps.appendLedger(
      buildLedgerEntry({ now, kind: "request_queued", requestId: env.request_id, peer: sender, scope: env.scope })
    );
    return "queued";
  }

  if (env.kind === "disclosure") {
    const result = await verifyGrant({
      grant: env.grant,
      signature: env.signature,
      expectedOwner: sender,
      expectedAudience: self,
      payload: env.payload,
      now,
    });
    const reason = !result.ok
      ? result.reason
      : env.grant.request_id !== env.request_id
        ? "request_mismatch"
        : null;
    if (reason) {
      await deps.appendLedger(
        buildLedgerEntry({ now, kind: "disclosure_rejected", requestId: env.request_id, peer: sender, reason })
      );
      return "rejected";
    }
    const stored = await deps.storeDisclosure(env, sender);
    if (!stored) return "duplicate";
    await deps.appendLedger(
      buildLedgerEntry({
        now,
        kind: "disclosure_stored",
        requestId: env.request_id,
        peer: sender,
        scope: env.grant.scope,
        payloadHash: env.grant.payload_hash,
      })
    );
    return "stored";
  }

  if (env.kind === "denial") {
    await deps.appendLedger(
      buildLedgerEntry({ now, kind: "denial_received", requestId: env.request_id, peer: sender, reason: env.reason })
    );
    return "noted";
  }

  await deps.appendLedger(
    buildLedgerEntry({ now, kind: "pending_received", requestId: env.request_id, peer: sender })
  );
  return "noted";
}
```

新建 `workers/xmtp-member/src/consentActions.ts`：

```ts
import { freeBusy, type CalendarEvent } from "./calendar.ts";
import type { ConsentQueue } from "./consentQueue.ts";
import { encodeEnvelope } from "./envelope.ts";
import { buildGrant, signGrant } from "./grant.ts";
import { buildLedgerEntry, type LedgerEntry } from "./ledger.ts";

export const CONSENT_TTL_MS = 10 * 60 * 1000;

export interface ConsentActionDeps {
  selfAddress: string;
  privateKey: string;
  queue: ConsentQueue;
  loadCalendar: () => Promise<CalendarEvent[]>;
  send: (conversationId: string, text: string) => Promise<void>;
  appendLedger: (entry: LedgerEntry) => Promise<void>;
  now: () => Date;
}

export type ActionResult = { ok: true } | { ok: false; reason: "not_pending" | "send_failed" };

export async function approveConsent(requestId: string, deps: ConsentActionDeps): Promise<ActionResult> {
  const item = await deps.queue.claim(requestId, "approved");
  if (!item) return { ok: false, reason: "not_pending" };
  const now = deps.now();
  const payload = freeBusy(await deps.loadCalendar(), item.constraints);
  const grant = buildGrant({
    owner: deps.selfAddress,
    audience: item.requester,
    requestId: item.request_id,
    scope: item.scope,
    constraints: item.constraints,
    payload,
    now,
  });
  const signature = await signGrant(grant, deps.privateKey);
  try {
    await deps.send(
      item.conversation_id,
      encodeEnvelope({ kind: "disclosure", request_id: item.request_id, to: item.requester, grant, signature, payload })
    );
  } catch {
    await deps.appendLedger(
      buildLedgerEntry({ now, kind: "send_failed", requestId: item.request_id, peer: item.requester, scope: item.scope })
    );
    return { ok: false, reason: "send_failed" };
  }
  await deps.appendLedger(
    buildLedgerEntry({
      now,
      kind: "consent_approved",
      requestId: item.request_id,
      peer: item.requester,
      scope: item.scope,
      payloadHash: grant.payload_hash,
    })
  );
  return { ok: true };
}

export async function denyConsent(
  requestId: string,
  reason: "owner_denied" | "expired",
  deps: ConsentActionDeps
): Promise<ActionResult> {
  const item = await deps.queue.claim(requestId, reason === "expired" ? "expired" : "denied");
  if (!item) return { ok: false, reason: "not_pending" };
  const now = deps.now();
  try {
    await deps.send(
      item.conversation_id,
      encodeEnvelope({ kind: "denial", request_id: item.request_id, to: item.requester, reason })
    );
  } catch {
    await deps.appendLedger(
      buildLedgerEntry({ now, kind: "send_failed", requestId: item.request_id, peer: item.requester, scope: item.scope })
    );
    return { ok: false, reason: "send_failed" };
  }
  await deps.appendLedger(
    buildLedgerEntry({
      now,
      kind: reason === "expired" ? "consent_expired" : "consent_denied",
      requestId: item.request_id,
      peer: item.requester,
      scope: item.scope,
      reason,
    })
  );
  return { ok: true };
}

export async function expireDue(deps: ConsentActionDeps): Promise<number> {
  const due = deps.queue.dueForExpiry(deps.now(), CONSENT_TTL_MS);
  let count = 0;
  for (const item of due) {
    const result = await denyConsent(item.request_id, "expired", deps);
    if (result.ok) count += 1;
  }
  return count;
}
```

- [ ] **Step 4: 运行，确认通过**

```bash
cd workers/xmtp-member && node --experimental-strip-types --test src/consentFlow.test.ts
```

Expected: `ℹ pass 10`，`ℹ fail 0`。

- [ ] **Step 5: 提交**

```bash
git add workers/xmtp-member/src/handleEnvelope.ts workers/xmtp-member/src/consentActions.ts workers/xmtp-member/src/consentFlow.test.ts
git commit -m "$(cat <<'EOF'
[网关] 请求入队、越界拒绝、批准签发与披露核验

- Added: handleEnvelope、approveConsent、denyConsent、expireDue
- Handoff: 批准先 claim 再取数再签名再发送；发送失败记 send_failed，不会回滚成 pending
EOF
)"
```

---

---

### 等待点：B12 乙的控制台文件已推送

```bash
git pull --rebase origin feat/owner-gateway
ls workers/xmtp-member/src/outgoingRequest.ts workers/xmtp-member/src/localGuard.ts workers/xmtp-member/src/ownerConsole.ts workers/xmtp-member/src/ownerConsoleHtml.ts
```

四个文件都列出来才做 Task 12。报 `No such file` 就停下等乙。

---

### Task 12: 把网关接进成员进程入口

**Files:**
- Modify: `workers/xmtp-member/src/index.ts`（整文件替换）
- Modify: `workers/xmtp-member/.env.example`

- [ ] **Step 1: 整文件替换 `workers/xmtp-member/src/index.ts`**

```ts
import { existsSync } from "node:fs";
import { appendFile, mkdir } from "node:fs/promises";
import path from "node:path";
import { Agent } from "@xmtp/agent-sdk";
import { buildBindingPayload } from "./binding.ts";
import { loadCalendar } from "./calendar.ts";
import { approveConsent, denyConsent, expireDue, type ConsentActionDeps } from "./consentActions.ts";
import { ConsentQueue } from "./consentQueue.ts";
import { decodeEnvelope, encodeEnvelope, ENVELOPE_PREFIX } from "./envelope.ts";
import { missingMembers } from "./groupMembers.ts";
import { fetchHubGroupId, resolveGroupPlan } from "./groupPlan.ts";
import { handleEnvelope, type HandleEnvelopeDeps } from "./handleEnvelope.ts";
import { appendInboxOnce, readInbox } from "./inbox.ts";
import { appendLedger, buildLedgerEntry, readLedger, type LedgerEntry } from "./ledger.ts";
import {
  createLocalLoop,
  loadLoopState,
  saveLoopState,
  type LocalLoopState,
} from "./localLoop.ts";
import { completeWithFetch } from "./modelComplete.ts";
import { onInboundText } from "./onInboundText.ts";
import { buildRequestEnvelope } from "./outgoingRequest.ts";
import type { OwnerNote } from "./ownerNote.ts";
import { createOwnerConsoleServer } from "./ownerConsole.ts";
import { ownerNotesBindAddress, readOwnerNotesFile } from "./ownerNotesHttp.ts";
import { loadPolicy } from "./policy.ts";
import { isAllowedSender, parseAllowedSenders } from "./senderPolicy.ts";

export interface MemberConfig {
  selfName?: string;
  participants?: string[];
  xmtpWalletKey?: string;
  xmtpDbEncryptionKey?: string;
  xmtpEnv?: string;
  xmtpPeerAddresses?: string[];
  xmtpGroupId?: string;
  allowedSenders: string[];
  hubBaseUrl?: string;
  hubToken?: string;
  hubRoomId?: string;
  loopStatePath?: string;
  memberModelApiKey?: string;
  memberModelUrl?: string;
  approvedDays?: string[];
  sensitiveKeywords?: string[];
  ownerNotesPort?: string;
  calendarFile?: string;
}

function splitList(raw: string | undefined): string[] {
  return (raw || "")
    .split(",")
    .map((s) => s.trim())
    .filter(Boolean);
}

export function loadConfigFromEnv(): MemberConfig {
  const participants = splitList(process.env.XMTP_PARTICIPANTS);
  const approvedDays = splitList(process.env.XMTP_APPROVED_DAYS);
  const sensitiveKeywords = splitList(process.env.XMTP_SENSITIVE_KEYWORDS);

  const defaultStatePath = existsSync("workers/xmtp-member")
    ? path.resolve("workers/xmtp-member/.state/loop.json")
    : path.resolve(".state/loop.json");

  return {
    selfName: process.env.XMTP_SELF_NAME || "Codex",
    participants: participants.length > 0 ? participants : ["Codex", "Claude"],
    xmtpWalletKey: process.env.XMTP_WALLET_KEY,
    xmtpDbEncryptionKey: process.env.XMTP_DB_ENCRYPTION_KEY,
    xmtpEnv: process.env.XMTP_ENV || "dev",
    xmtpPeerAddresses: splitList(process.env.XMTP_PEER_ADDRESSES).map((s) => s.toLowerCase()),
    xmtpGroupId: process.env.XMTP_GROUP_ID || undefined,
    allowedSenders: parseAllowedSenders(process.env.XMTP_ALLOWED_SENDERS),
    hubBaseUrl: process.env.HUB_BASE_URL,
    hubToken: process.env.HUB_TOKEN,
    hubRoomId: process.env.HUB_ROOM_ID,
    loopStatePath: process.env.XMTP_LOOP_STATE_PATH || defaultStatePath,
    memberModelApiKey: process.env.MEMBER_MODEL_API_KEY,
    memberModelUrl: process.env.MEMBER_MODEL_URL,
    approvedDays: approvedDays.length > 0 ? approvedDays : undefined,
    sensitiveKeywords: sensitiveKeywords.length > 0 ? sensitiveKeywords : undefined,
    ownerNotesPort: process.env.OWNER_NOTES_PORT,
    calendarFile: process.env.MAPR_CALENDAR_FILE || undefined,
  };
}

export async function appendOwnerNote(
  filePath: string,
  note: OwnerNote
): Promise<void> {
  await mkdir(path.dirname(filePath), { recursive: true });
  await appendFile(filePath, JSON.stringify(note) + "\n", "utf8");
}

export async function bindGroupToHub(
  hubBaseUrl: string,
  hubRoomId: string,
  hubToken: string,
  groupId: string
): Promise<void> {
  const payload = buildBindingPayload({ xmtpGroupId: groupId });
  const url = `${hubBaseUrl.replace(/\/$/, "")}/api/v1/rooms/${hubRoomId}/xmtp-binding`;

  const resp = await fetch(url, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${hubToken}`,
    },
    body: JSON.stringify(payload),
  });

  if (!resp.ok) {
    throw new Error(`Failed to bind group to hub: HTTP status ${resp.status}`);
  }
}

export async function startMember(): Promise<void> {
  const config = loadConfigFromEnv();
  if (config.allowedSenders.length === 0) {
    throw new Error("XMTP_ALLOWED_SENDERS is empty; refusing to start");
  }
  if (!config.xmtpWalletKey) {
    throw new Error("XMTP_WALLET_KEY is required");
  }
  const agent = await Agent.createFromEnv();
  const selfAddress = (agent.address || "").toLowerCase();

  const statePath =
    config.loopStatePath ||
    (existsSync("workers/xmtp-member")
      ? path.resolve("workers/xmtp-member/.state/loop.json")
      : path.resolve(".state/loop.json"));
  const stateDir = path.dirname(statePath);
  const ownerNotesPath = path.join(stateDir, "owner-notes.jsonl");
  const ledgerPath = path.join(stateDir, "ledger.jsonl");
  const policyPath = path.join(stateDir, "policy.json");
  const consentsPath = path.join(stateDir, "consents.json");
  const inboxPath = path.join(stateDir, "inbox.json");
  const calendarPath = config.calendarFile || path.join(stateDir, "calendar.json");

  const policy = await loadPolicy(policyPath);
  const queue = new ConsentQueue(consentsPath);
  await queue.load();

  let loopState: LocalLoopState;
  if (existsSync(statePath)) {
    loopState = await loadLoopState(statePath);
  } else {
    loopState = createLocalLoop({
      participants: config.participants || ["Codex", "Claude"],
      selfName: config.selfName || "Codex",
    });
  }

  const complete =
    config.memberModelApiKey && config.memberModelUrl
      ? (prompt: string) =>
          completeWithFetch({
            apiKey: config.memberModelApiKey!,
            url: config.memberModelUrl!,
            prompt,
          })
      : undefined;

  const writeLedger = (entry: LedgerEntry) => appendLedger(ledgerPath, entry);

  async function sendToConversation(conversationId: string, text: string): Promise<void> {
    let ctx = await agent.getConversationContext(conversationId);
    if (!ctx) {
      await agent.client.conversations.syncAll();
      ctx = await agent.getConversationContext(conversationId);
    }
    if (!ctx) throw new Error("conversation not found");
    await ctx.conversation.sendText(text);
  }

  async function ensurePeersInGroup(groupId: string, peers: string[]): Promise<void> {
    if (peers.length === 0) return;
    await agent.client.conversations.syncAll();
    const ctx = await agent.getConversationContext(groupId);
    if (!ctx || !ctx.isGroup()) {
      console.warn(`[member] group ${groupId} not found locally; is this member in the group?`);
      return;
    }
    const members = await ctx.conversation.members();
    const present = members.flatMap((m) => m.accountIdentifiers.map((i) => i.identifier));
    const missing = missingMembers(present, peers);
    if (missing.length === 0) return;
    await agent.addMembersWithAddresses(ctx.conversation, missing as `0x${string}`[]);
    console.log(`[member] added ${missing.length} peer(s) to group`);
  }

  const envelopeDeps: HandleEnvelopeDeps = {
    selfAddress,
    policy,
    queue,
    send: sendToConversation,
    appendLedger: writeLedger,
    storeDisclosure: (disclosure, from) =>
      appendInboxOnce(inboxPath, {
        received_at: new Date().toISOString(),
        from,
        request_id: disclosure.request_id,
        grant_id: disclosure.grant.grant_id,
        payload: disclosure.payload,
      }),
    now: () => new Date(),
  };

  const actionDeps: ConsentActionDeps = {
    selfAddress,
    privateKey: config.xmtpWalletKey,
    queue,
    loadCalendar: () => loadCalendar(calendarPath),
    send: sendToConversation,
    appendLedger: writeLedger,
    now: () => new Date(),
  };

  let activeGroupId: string | null = null;

  const consoleServer = createOwnerConsoleServer({
    selfAddress,
    queue,
    readLedger: () => readLedger(ledgerPath),
    readInbox: () => readInbox(inboxPath),
    readOwnerNotes: () => readOwnerNotesFile(ownerNotesPath),
    approve: (id) => approveConsent(id, actionDeps),
    deny: (id) => denyConsent(id, "owner_denied", actionDeps),
    sendRequest: async (body) => {
      if (!activeGroupId) return { ok: false, reason: "no_group" };
      const built = buildRequestEnvelope(body, selfAddress);
      if (!built.ok) return { ok: false, reason: built.reason };
      await sendToConversation(activeGroupId, encodeEnvelope(built.envelope));
      await writeLedger(
        buildLedgerEntry({
          now: new Date(),
          kind: "request_sent",
          requestId: built.envelope.request_id,
          peer: built.envelope.to,
          scope: built.envelope.scope,
        })
      );
      return { ok: true, request_id: built.envelope.request_id };
    },
  });
  const consolePort = parseInt(config.ownerNotesPort || "8787", 10);
  consoleServer.listen(consolePort, ownerNotesBindAddress());

  const expiryTimer = setInterval(() => {
    expireDue(actionDeps).catch((err) => console.error("Expiry error:", err.message));
  }, 30_000);
  expiryTimer.unref();

  agent.on("text", async (ctx: any) => {
    const sender = ((await ctx.getSenderAddress()) || "").toLowerCase();
    if (sender && sender === selfAddress) return;
    if (!isAllowedSender(sender, config.allowedSenders)) {
      await writeLedger(
        buildLedgerEntry({
          now: new Date(),
          kind: "sender_dropped",
          peer: sender || null,
          reason: "sender_not_allowed",
        })
      );
      return;
    }

    const text =
      typeof ctx.message?.content === "string"
        ? ctx.message.content
        : ctx.message?.content?.text || "";

    const envelope = decodeEnvelope(text);
    if (envelope) {
      try {
        await handleEnvelope({ envelope, sender, conversationId: ctx.conversation.id }, envelopeDeps);
      } catch (err: any) {
        console.error("Envelope error:", err.message);
      }
      return;
    }
    if (text.startsWith(ENVELOPE_PREFIX)) return;

    try {
      loopState = await onInboundText({
        state: loopState,
        text,
        complete,
        approvedDays: config.approvedDays,
        sensitiveKeywords: config.sensitiveKeywords,
        recordOwner: async (note) => {
          await appendOwnerNote(ownerNotesPath, note);
        },
        sendText: async (body: string) => {
          await ctx.conversation.sendText(body);
        },
        saveState: async (nextState: LocalLoopState) => {
          await saveLoopState(statePath, nextState);
        },
      });
    } catch (err: any) {
      console.error("Worker process error:", err.message);
    }
  });

  const hubConfigured = Boolean(config.hubBaseUrl && config.hubRoomId && config.hubToken);
  const hubGroupId = hubConfigured
    ? await fetchHubGroupId({
        hubBaseUrl: config.hubBaseUrl!,
        hubRoomId: config.hubRoomId!,
        hubToken: config.hubToken!,
      })
    : null;

  const plan = resolveGroupPlan({
    envGroupId: config.xmtpGroupId,
    hubGroupId,
    peerAddresses: config.xmtpPeerAddresses || [],
  });

  if (plan.action === "create") {
    const group = await agent.createGroupWithAddresses(
      (config.xmtpPeerAddresses || []) as `0x${string}`[]
    );
    activeGroupId = group.id;
    console.log(`[member] created group ${group.id}`);
    if (hubConfigured) {
      await bindGroupToHub(config.hubBaseUrl!, config.hubRoomId!, config.hubToken!, group.id);
    }
  } else if (plan.action === "use") {
    activeGroupId = plan.groupId;
    console.log(`[member] using group ${plan.groupId} from ${plan.source}`);
    await ensurePeersInGroup(plan.groupId, config.xmtpPeerAddresses || []);
  } else {
    console.log("[member] no group configured; listening only");
  }

  console.log(`[member] address ${selfAddress}`);
  console.log(`[member] owner console http://127.0.0.1:${consolePort}/`);
  await agent.start();
}

if (process.argv[1] && process.argv[1].endsWith("index.ts")) {
  startMember().catch((err) => {
    console.error("Worker process error:", err.message);
    process.exit(1);
  });
}
```

和阶段 A 版本相比：

1. 启动时加载 `policy.json`、`consents.json`，没有就用空策略、空队列。
2. 收到消息后，白名单检查之后先试着按 `MAPR1` 解码。解码成功走 `handleEnvelope`，不进模型；以 `MAPR1 ` 开头但解码失败的直接丢弃，也不进模型。其余普通文本仍走原来的 `onInboundText`。
3. 原来的 `/owner-notes` 服务换成 `createOwnerConsoleServer`，端口不变。
4. 每 30 秒检查一次超时的待批准请求，超时自动回复 `expired`。
5. 发往群的消息统一走 `sendToConversation`：本地找不到群就先 `syncAll` 再找。

- [ ] **Step 2: `.env.example` 追加一行**

在 `workers/xmtp-member/.env.example` 末尾追加：

```text
MAPR_CALENDAR_FILE=
```

- [ ] **Step 3: 类型检查与全部测试**

```bash
cd workers/xmtp-member && npm run typecheck && npm test
```

Expected: typecheck 无输出；`ℹ pass 85`，`ℹ fail 0`。

- [ ] **Step 4: 提交**

```bash
git add workers/xmtp-member/src/index.ts workers/xmtp-member/.env.example
git commit -m "$(cat <<'EOF'
[网关] 成员进程接入策略、同意队列与本机控制台

- Changed: MAPR1 消息走网关流程，不进模型
- Added: 30 秒一次的超时处理；MAPR_CALENDAR_FILE 配置
- Handoff:
 - 本机文件：.state/policy.json、consents.json、inbox.json、calendar.json、ledger.jsonl
 - 这些文件都在已忽略的 .state 目录，不许提交
EOF
)"
```

---

---

### Task 14: 单机双网关实测（XMTP dev 网络）

这一步在一台电脑上起两个网关（Alice 和 Bob），不需要 Hub，也不需要浏览器。所有文件都写在 `/tmp/mapr-drill`，不进仓库。

**Files:**
- Modify: `ACCEPTANCE_CHECKLIST.md`（新增一行）

- [ ] **Step 1: 生成两个临时身份和配置**

```bash
mkdir -p /tmp/mapr-drill/a /tmp/mapr-drill/b/state
cd workers/xmtp-member && node --input-type=module -e '
import { generatePrivateKey, privateKeyToAccount } from "viem/accounts";
import { randomBytes } from "node:crypto";
import { writeFileSync } from "node:fs";
const ka = generatePrivateKey(), kb = generatePrivateKey();
const a = privateKeyToAccount(ka).address.toLowerCase(), b = privateKeyToAccount(kb).address.toLowerCase();
const base = (dir, key, port) => [
  `XMTP_WALLET_KEY=${key}`, `XMTP_DB_ENCRYPTION_KEY=0x${randomBytes(32).toString("hex")}`, "XMTP_ENV=dev",
  `XMTP_DB_DIRECTORY=${dir}/xmtp-db`, `XMTP_LOOP_STATE_PATH=${dir}/state/loop.json`, `OWNER_NOTES_PORT=${port}`];
writeFileSync("/tmp/mapr-drill/a/.env", [...base("/tmp/mapr-drill/a", ka, 8801), `XMTP_ALLOWED_SENDERS=${b}`, `XMTP_PEER_ADDRESSES=${b}`].join("\n") + "\n");
writeFileSync("/tmp/mapr-drill/b/.env", [...base("/tmp/mapr-drill/b", kb, 8802), `XMTP_ALLOWED_SENDERS=${a}`].join("\n") + "\n");
writeFileSync("/tmp/mapr-drill/b/state/policy.json", JSON.stringify({ allow: { [a]: ["calendar.free_busy"] }, max_range_days: 14 }));
writeFileSync("/tmp/mapr-drill/b/state/calendar.json", JSON.stringify([{ start: "2026-10-06T09:00:00Z", end: "2026-10-06T10:00:00Z", title: "绝密-和律师面谈", notes: "带房产证" }]));
writeFileSync("/tmp/mapr-drill/addrs.json", JSON.stringify({ a, b }));
console.log("ok");
'
```

Expected: `ok`。

- [ ] **Step 2: 开两个终端，先启动 Bob，再启动 Alice**

终端 1（Bob）：

```bash
cd workers/xmtp-member && node --env-file=/tmp/mapr-drill/b/.env --experimental-strip-types src/index.ts
```

Expected：`[member] no group configured; listening only` 和 `[member] owner console http://127.0.0.1:8802/`。

等 Bob 打印出这两行，再开终端 2（Alice）：

```bash
cd workers/xmtp-member && node --env-file=/tmp/mapr-drill/a/.env --experimental-strip-types src/index.ts
```

Expected：`[member] created group <一串十六进制>` 和 `[member] owner console http://127.0.0.1:8801/`。

- [ ] **Step 3: Alice 申请 Bob 下周的忙闲**

开终端 3：

```bash
B=$(node -p 'require("/tmp/mapr-drill/addrs.json").b')
curl -s -X POST http://127.0.0.1:8801/api/requests -H 'X-MAPR-Owner: 1' -H 'Content-Type: application/json' \
  -d "{\"to\":\"$B\",\"scope\":\"calendar.free_busy\",\"purpose\":\"约下周的会\",\"date_from\":\"2026-10-05\",\"date_to\":\"2026-10-09\"}"
```

Expected：`{"ok":true,"request_id":"<一串 id>"}`。记下这个 id。

等 10 秒，然后：

```bash
curl -s http://127.0.0.1:8802/api/state
```

Expected：`consents` 里有一条 `"status":"pending"`，`requester` 是 Alice 的地址。

- [ ] **Step 4: Bob 在浏览器里批准**

用浏览器打开 `http://127.0.0.1:8802/`，在"待批准"里点"允许一次"。

等 10 秒，然后：

```bash
curl -s http://127.0.0.1:8801/api/state
```

Expected：
- `inbox` 里有一条，`payload.busy` 是 `[{"start":"2026-10-06T09:00:00.000Z","end":"2026-10-06T10:00:00.000Z"}]`。
- 整段输出里搜不到 `律师`、`房产证`、`绝密`。
- `ledger` 里依次有 `request_sent`、`pending_received`、`disclosure_stored`。

- [ ] **Step 5: 越界请求自动拒绝**

```bash
B=$(node -p 'require("/tmp/mapr-drill/addrs.json").b')
curl -s -X POST http://127.0.0.1:8801/api/requests -H 'X-MAPR-Owner: 1' -H 'Content-Type: application/json' \
  -d "{\"to\":\"$B\",\"scope\":\"calendar.free_busy\",\"purpose\":\"看整月\",\"date_from\":\"2026-10-01\",\"date_to\":\"2026-10-30\"}"
sleep 10
curl -s http://127.0.0.1:8801/api/state
curl -s http://127.0.0.1:8802/api/state
```

Expected：Alice 的 `ledger` 最后一条是 `"kind":"denial_received"`、`"reason":"out_of_scope"`；Bob 的 `consents` 里没有新增 pending；Bob 的 `ledger` 最后一条是 `request_denied`。

- [ ] **Step 6: 重复批准与缺请求头**

```bash
curl -s -X POST http://127.0.0.1:8802/api/consents/<Step 3 的 id>/approve -H 'X-MAPR-Owner: 1'
curl -s -o /dev/null -w "%{http_code}\n" -X POST http://127.0.0.1:8802/api/consents/<Step 3 的 id>/deny
```

Expected：第一条 `{"ok":false,"reason":"not_pending"}`；第二条 `403`。

- [ ] **Step 7: 收尾**

两个终端各按 Ctrl+C。然后删掉临时文件：

```bash
rm -rf /tmp/mapr-drill
```

- [ ] **Step 8: 记录**

在 `ACCEPTANCE_CHECKLIST.md` 的 `## Current Status` 表格末尾加一行（日期填当天）：

```text
| Owner gateway consent (dev) | Passed 2026-MM-DD | 单机双网关：批准后只收到时段，越界自动拒绝，重复批准被拒，缺请求头 403。 |
```

任一步 Expected 不符合，就不要写 Passed，把现象写进备注，停止并报告人类。

```bash
git add ACCEPTANCE_CHECKLIST.md
git commit -m "$(cat <<'EOF'
[文档] 记录单机双网关同意流程实测

- Changed: 验收清单新增 Owner gateway consent 行
EOF
)"
```

---

---

## 6. 后续：阶段 C、D（原文）

这两个阶段归你。开工前必须按下面"展开规则"先展开成完整计划。

# 阶段 C 到 E 任务书

> 这不是可以直接照抄执行的计划。它定义了每个阶段要做什么、改哪些文件、接口长什么样、测试要验证什么、验收标准是什么。
> 执行任何一个阶段之前，必须先按本文第 1 节"展开规则"把该阶段展开成和阶段 A、B 同样粒度的完整计划。

---

## 1. 展开规则

### 1.1 为什么不直接写成完整代码

阶段 C 到 E 的代码依赖阶段 A、B 落地后的实际文件，也依赖几项还没做的人类决定：
- 第三方日历、邮箱用哪家；
- OAuth 凭据由谁申请；
- Hub 要不要加新表。

现在就写出逐行代码，等于替人类做决定。代码也无法在当前仓库里验证，照抄会卡住。

### 1.2 谁来展开

- **展开**必须由能力较强的模型或人类完成。展开者要先在临时副本里把代码和测试真正跑通，再写进计划，和阶段 A、B 的做法一样。
- **执行**展开后的计划，可以交给较弱的模型。

### 1.3 展开步骤

1. 确认上一阶段的验收全部通过，并已记录在 `ACCEPTANCE_CHECKLIST.md`。
2. 确认本阶段"人类决定"一栏的每一项都拿到了明确答复，写进 `decisions.md`。
3. 用 `git worktree add /tmp/mapr-<阶段> HEAD` 建临时副本，在副本里按本任务书实现并跑通全部测试。
4. 按 `docs/superpowers/plans/2026-09-23-B-主人网关与单次同意.md` 的格式写 `docs/superpowers/plans/<日期>-<阶段>-<名称>.md`。要求：
   - 每个任务都有完整代码、完整测试、准确的运行命令和预期输出；
   - 不许出现"类似上一个任务""补充错误处理"这类说法。
5. 删除临时副本：`git worktree remove /tmp/mapr-<阶段> --force`。
6. 把新计划提交，更新 `CLAUDE.md` 入口。

---

## 2. 阶段 C：协调者与计分板

对标 AgentPicnic 的 F9（新成员加入审批）、F11（协调者）、F12（计分板）。

### 2.1 人类决定

| 编号 | 问题 | 默认建议 |
| --- | --- | --- |
| C-Q1 | 执行者收到任务是否要主人逐次批准？ | 否。`policy.json` 里给协调者地址加 `task.run` 范围即视为长期批准；没有就自动 `out_of_scope` |
| C-Q2 | 协调者用哪个模型验收结果？ | 复用 `MEMBER_MODEL_URL` 与 `MEMBER_MODEL_API_KEY` |
| C-Q3 | 计分板是否上报 Hub？ | 本阶段不上报，只在本机控制台显示。上报需要新表，另走数据库审批 |

### 2.2 协议扩展

在 `workers/xmtp-member/src/envelope.ts` 的 `Envelope` 联合类型里增加三种。

`request_id` 字段在这三种消息里就是任务编号。沿用这个字段名，是为了让现有的 `decodeEnvelope` 公共校验继续生效。

```ts
export interface TaskEnvelope {
  kind: "task";
  request_id: string;     // 任务编号
  to: string;             // 执行者地址，小写
  goal: string;           // 1..500 字
  acceptance: string[];   // 1..5 条，每条 1..200 字
  round: number;          // 1..3，第几轮
}

export interface ResultEnvelope {
  kind: "result";
  request_id: string;
  to: string;             // 协调者地址
  round: number;
  summary: string;        // 1..2000 字
  evidence: string[];     // 0..10 条，每条 1..500 字
}

export interface VerdictEnvelope {
  kind: "verdict";
  request_id: string;
  to: string;             // 执行者地址
  round: number;
  accepted: boolean;
  challenge: string;      // accepted 为 false 时 1..500 字，为 true 时空串
}
```

`KNOWN_SCOPES` 增加 `"task.run"`。

`decodeEnvelope` 要逐字段校验上面的长度与取值范围，超出就返回 `null`。

**测试必须覆盖：**
- 三种类型各一次往返编解码；
- `goal` 为 501 字时返回 `null`；
- `acceptance` 为空数组时返回 `null`；
- `round` 为 4 时返回 `null`；
- `accepted` 为 false 且 `challenge` 为空时返回 `null`。

### 2.3 新文件

| 文件 | 职责 | 主要导出 |
| --- | --- | --- |
| `src/coordinator.ts` | 协调者的纯逻辑：根据结果与验收标准生成裁决，决定下一步 | `buildVerdictPrompt(task, result): string`、`parseVerdict(modelText): {accepted: boolean; challenge: string} \| null`、`nextStep(task, verdict): "done" \| "retry" \| "escalate"` |
| `src/executor.ts` | 执行者的纯逻辑：根据任务生成结果 | `buildTaskPrompt(task): string`、`buildResult(task, modelText): ResultEnvelope` |
| `src/taskStore.ts` | 协调者本机的任务表，存在 `.state/tasks.json` | `class TaskStore { add; get; list; recordResult; recordVerdict }` |
| `src/scoreboard.ts` | 从账本计算计分 | `scoreboard(entries: LedgerEntry[]): Array<{peer: string; accepted: number; rejected: number}>` |
| `src/joinRequest.ts` | 入群申请的纯逻辑 | `buildJoinRequest(candidate, proposedScopes)`、`applyJoinApproval(policy, candidate, scopes): Policy` |

### 2.4 行为规则

1. 协调者的主人在控制台"新建任务"里填：执行者地址、目标、验收标准。网关发 `task`，`round` 为 1，账本记 `task_sent`。
2. 执行者网关收到 `task`：
   - 先按 `checkRequest(policy, sender, {scope: "task.run", ...})` 判断范围。`task.run` 没有日期范围，`checkRequest` 需要对这个范围跳过日期检查。
   - 不允许就回 `denial(out_of_scope)`。
   - 允许就调用模型，只把 `goal` 和 `acceptance` 放进提示词，不放任何本机文件内容；再发 `result`。
3. 协调者收到 `result`：
   - 调用模型，提示词只含任务与结果；
   - 模型必须输出 `{"accepted": true|false, "challenge": "..."}` 这样的 JSON；
   - `parseVerdict` 解析失败按 `escalate` 处理。
4. `nextStep` 的规则：
   - `accepted` 为 true，返回 `done`；
   - `accepted` 为 false 且 `round` 小于 3，返回 `retry`：发 `verdict`，再发 `round + 1` 的 `task`；
   - `accepted` 为 false 且 `round` 等于 3，返回 `escalate`：不再发 `task`，账本记 `task_escalated`，控制台提示主人。
5. 计分：账本新增 `verdict_sent` 类型，`reason` 字段写 `accepted` 或 `rejected`，`peer` 写执行者。`scoreboard` 按 `peer` 汇总。计分只算被接受的成果，不算消息条数。
6. 入群申请：
   - 任何白名单成员可以发 `join_request`，内容是候选地址和建议范围；
   - 在被请求方主人的控制台里批准后，网关先调用 `addMembersWithAddresses` 把候选人加进群，再把候选人和批准的范围写进 `policy.json`；
   - 同时要提示主人：还要把候选人手工加进 `.env` 的 `XMTP_ALLOWED_SENDERS`。是否自动写 `.env` 需要人类另行决定，默认不自动写。

### 2.5 测试清单（展开时逐条写成测试）

- `parseVerdict` 接受合法 JSON；拒绝缺字段、非 JSON、`accepted` 为字符串的情况。
- `nextStep`：
  - round 1 被拒，返回 `retry`；
  - round 3 被拒，返回 `escalate`；
  - 任一轮被接受，返回 `done`。
- 执行者提示词里不出现本机 `calendar.json` 的任何内容。做法是在测试里准备一个含"绝密"的日历，再断言提示词里没有"绝密"。
- 协调者没有 `task.run` 授权时，执行者回 `out_of_scope`，也不调用模型（用计数器断言调用次数为 0）。
- `scoreboard`：
  - 两个执行者各被接受 2 次、1 次，排序正确；
  - 被拒次数单独计数。
- `applyJoinApproval` 只加入批准的范围，不加入候选人自己提出但被主人去掉的范围。

### 2.6 验收

在一台机器上起三个网关：协调者、执行者甲、执行者乙，都在 XMTP `dev` 网络。协调者给甲派"列出三个周末出游地点，每个附一句理由"，验收标准写"必须恰好三个地点"。测试模型可以用一个本机假模型服务，返回固定文本，先返回两个地点，第二轮返回三个。

预期：
1. 第一轮被质疑，第二轮通过；
2. 控制台计分板显示甲 1 次接受；
3. 执行者乙没有 `task.run` 授权，协调者给乙派任务时收到 `out_of_scope`。

---

## 3. 阶段 D：真实连接器

对标 F7（由已有代理代取数据）、F8（连接隔离）。**会接触真实个人数据，开工前必须由人类单独批准。**

### 3.1 人类决定

| 编号 | 问题 | 说明 |
| --- | --- | --- |
| D-Q1 | 日历接哪家？ | Google Calendar、Outlook 或 CalDAV 选一家；决定 OAuth 客户端由谁申请 |
| D-Q2 | 邮箱接哪家？ | Gmail 或 Outlook；只读权限 |
| D-Q3 | 凭据放哪？ | 建议放 macOS 钥匙串或本机 `.state/credentials/` 并设 0600 权限；不许进 git，不许进群，不许进 Hub |
| D-Q4 | 是否先做安全审查再上线？ | 建议必须做，使用 security-review 子代理审查整个 D 阶段的改动 |

### 3.2 连接器接口

新建 `workers/xmtp-member/src/connectors/types.ts`：

```ts
import type { DateRange } from "../envelope.ts";

export interface Connector {
  id: string;                       // 例如 "google-calendar"
  scopes: string[];                 // 本连接器能提供的范围
  fetch(scope: string, constraints: unknown): Promise<unknown>; // 返回的对象必须只含批准字段
}

export interface EmailReceiptConstraints {
  query: string;        // 例如 "from:12306 subject:电子客票"
  date_from: string;
  date_to: string;
}

export interface EmailReceiptPayload {
  scope: "email.receipt";
  subject: string;
  sent_at: string;
  attachment_name: string | null;
  attachment_sha256: string | null;
  body_excerpt: string;   // 最多 500 字
}
```

`consentActions.approveConsent` 改为：按 `item.scope` 找到对应连接器，调用 `fetch`。不再写死 `freeBusy`。

`KNOWN_SCOPES` 增加 `"email.receipt"`。

`checkRequest` 要按范围分别校验约束：
- `calendar.free_busy` 校验日期范围；
- `email.receipt` 校验 `query` 长度不超过 200、日期跨度不超过 `max_range_days`。

### 3.3 新文件

| 文件 | 职责 |
| --- | --- |
| `src/connectors/types.ts` | 上面的接口 |
| `src/connectors/registry.ts` | `findConnector(scope, connectors): Connector \| null` |
| `src/connectors/localCalendar.ts` | 把阶段 B 的 `calendar.ts` 包装成连接器，保证原有测试不变 |
| `src/connectors/googleCalendar.ts`（按 D-Q1 决定名称） | 只调用忙闲查询接口，不调用事件详情接口 |
| `src/connectors/gmailReceipt.ts`（按 D-Q2 决定名称） | 按 `query` 和日期搜一封最新邮件，只返回 `EmailReceiptPayload` 字段 |
| `src/connectors/redact.ts` | `excerpt(text, max)`：截断并去掉邮箱地址和手机号 |

### 3.4 测试清单

- 注册表找不到范围时，`approveConsent` 返回 `{ok: false, reason: "no_connector"}`，不发任何消息。
- 用假的 HTTP 客户端测试日历连接器：请求地址是忙闲接口，不是事件列表接口；返回值只含 `start`、`end`。
- 邮件连接器：
  - 假邮件里含收件人邮箱、手机号、其他邮件标题，输出里都不出现；
  - `body_excerpt` 不超过 500 字。
- 凭据文件权限不是 0600 时拒绝启动。
- 端到端复现 AgentPicnic 的"火车票收据"场景：甲的费用代理申请 `email.receipt`，`query` 限定 12306；乙批准；甲只收到一张收据的摘要和附件哈希。

### 3.5 验收

- 真实账号上跑一遍上面的端到端，截图控制台；
- security-review 子代理没有高危项；
- 人类确认后在 `ACCEPTANCE_CHECKLIST.md` 记录。

---

## 5. 与评估报告的对应

| 报告功能编号 | 阶段 |
| --- | --- |
| F1、F2 | A（白名单、群复用）、E（邀请） |
| F3、F4、F5、F6 | B |
| F7、F8 | D |
| F9、F11、F12 | C |
| F10 | XMTP 默认行为，E 阶段加一条测试 |
| F13、F14、F15、F17 | E |
| F16 | 不在本任务书，等 D、E 稳定后再评估 |

---

## 7. 原计划自检（原文，供核对）

阶段 A：

## 计划自检

- 任意成员改绑群（报告 S1）：Task 2。
- 成员进程不校验发送者（S2）：Task 3、4、6。
- 每次启动新建群（S6）：Task 5、6。
- 另一方的浏览器和成员进程没人拉进群：Task 5b、6、7 Step 6。
- XMTP 网络写死（S5）：Task 7。浏览器私钥明文（S4）不在本计划，见后续任务书的 E-4。
- 生产 522（A1）：Task 9。
- 双机演练（A2）：Task 10。
- 仓库卫生：Task 8。

阶段 B：

## 计划自检

- 主人代理作为守门人（对标 F3）：Task 8、12。
- 单次授权弹窗（F4）：Task 4、8、11。
- 按数据范围授权（F5）：Task 5、6。
- 越界追问拦截（F6）：Task 6、8（`calendar.attendees` 和超长日期都被拒）。
- 对方消息不进模型（报告 S3）：Task 12（`MAPR1` 消息不进 `onInboundText`；日历内容从不进 `composeReply`）。
- Hub 不参与、不保存内容：本计划不改 `backend/`。
