# 项目开发与实施规划书

本文档是多智能体项目协作室的统一实施规划。
文档整合了历史阶段成果，并以可信协作指导意见作为后续推进依据。

## 0. 当前执行入口（2026-09-23）

加密房间的收发，以及本地历史明文房间删除，已经完成。

- 执行文档：`docs/xmtp端到端加密-执行文档.md`
- 下一位的计划：`docs/superpowers/plans/2026-09-23-member-local-loop.md`
- 成员进程把轮次状态留在本机，入站正文不进 Hub。
- `_agent_` 内部通道保留。生产库未清理。不合并 `main`。

## 1. 历史阶段演进概况

项目开发分为三个主要发展阶段。

### 阶段一：初始 MVP 验证阶段（已完成，2026 年 6 月）

该阶段验证了基础通信能力。
实现了用户注册登录、WebSocket 房间聊天、基础审批模型与早期 A2A 消息转发。
完成了 Docker 容器化部署与自动化测试管线构建。

### 阶段二：v2 架构重构阶段（已完成，2026 年 8 月）

该阶段重构了底层协议与协作机制。
详细技术方案记录在 `PLAN_V2.md`。
主要完成项包括：
- 引入官方 `a2a-sdk`，替换早期非标准协议。
- 新增 `MessageLoop` 协作驱动，支持上下文重置与共识标识提取。
- 新增 `agent_gateway.py`，采用 ACP 模式拉起本地命令行工具。
- 前端重构为多面板架构，支持共识状态渲染与团队配置导出。
- 完成房间权限、场景模板、消息搜索与移动端适配等基础功能。

### 阶段三：代理间可信协作改造阶段（当前进行中，2026 年 9 月）

该阶段解决身份归属、数据防护与执行控制问题。
整体设计以 `docs/项目指导意见-代理间可信协作.md` 为核心指导。

## 2. 可信协作分阶段实施路径

可信协作改造按三个阶段推进。各阶段设置明确的准入与验收标准。

```text
┌─────────────────────────────────────────────────────────────┐
│ Phase 0: 统一基线与契约                                      │
│ 固定代码提交 | 补齐测试基线 | 梳理组件保留与迁移清单             │
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼─────────────────────────────┐
│ Phase 1: Mock 条件下建立受控可信通道                         │
│ 身份强绑定 | 会话鉴权 | 一次性授权凭证 | 取消联动机制 | 迟到隔离│
└──────────────────────────────┬──────────────────────────────┘
                               │
┌──────────────────────────────▼─────────────────────────────┐
│ Phase 2: 有限接入真实模型完成场景闭环                         │
│ 虚构日历协商最小闭环 | 超限主动暂停 | 主人私有汇报 | 审计对账   │
└─────────────────────────────────────────────────────────────┘
```

### Phase 0：统一基线与契约（当前阶段）

目标是建立统一的开发与验证基准。

核心任务：
- [x] 梳理项目全套开发文档，消除陈旧描述与相互矛盾的内容。
- [x] 确立远端 `origin/main`（提交哈希 `f1af097`）为公共开发基线。
- [ ] 明确旧适配器 `local_agent_adapter.py` 与网关 `agent_gateway.py` 的迁移路径。
- [ ] 补齐当前基线在真实运行环境下的完整测试覆盖率报告。
- [ ] 建立标准错误清单与双机联调测试方案。

验收标准：
两名开发者可以在同一版本代码上复现全部基线测试结果。

### Phase 1：Mock 条件下建立受控可信通道

目标是在不调用外部付费模型的前提下，跑通安全通信与控制机制。

核心任务：
- [ ] 在后端服务中将 `current_user` 与智能体身份进行强绑定校验。
- [ ] 增加房间与会话维度的访问鉴权逻辑，防止跨会话非法注入。
- [ ] 实现结构化一次性动作授权机制，杜绝事后补票。
- [ ] 打通任务取消状态与底层执行子进程的联动停止机制。
- [ ] 增加迟到输出隔离机制，确保停止后的结果直接丢弃。
- [ ] 验证单机双进程以及双机器断线重连下的状态恢复。

验收标准：
未获授权的消息无法进入执行层。
取消指令能够即时中止子进程。
断线重连后不产生重复操作。

### Phase 2：有限接入真实模型完成场景闭环（核心领域模型与受控逻辑已实现）

目标是跑通虚构日历协商的最小闭环。

核心任务：
- [x] 严格限制进出模型的数据范围，仅允许交换批准的空闲时段（CalendarOutboundFilter）。
- [x] 建立智能体主动请示逻辑，协商内容超出批准时自动挂起（MessageLoop.suspend/resume）。
- [x] 建立结构化双向共识校验机制，杜绝单方伪造共识（CandidateSlotProposal）。
- [x] 建立主人私有汇报通道，协商结果附带双方完整确认信息（PrivateReportService）。
- [x] 编写阶段 2 专属自动化测试套件并通过全部验证（test_calendar_negotiation.py）。
- [ ] 接入真实模型驱动双边协商逻辑与轮次费用消耗统计。

验收标准：
两位主人能够分别控制各自的智能体。
每一项信息披露均能追溯到主人的单次授权。
全程不触碰主人的真实私人日程。

## 3. 人类审批与决策边界

后续所有具体实施必须严格遵守下列规则：
- 涉及数据库表结构变更时，必须先提交方案并获得人类批准。
- 涉及系统对外核心 API 调整时，必须更新 `ARCHITECTURE.md`。
- 代码合并至 `main` 分支前，必须完成双机测试与代码审查。
- 禁止在未经人类审查前将功能部署到生产服务器。
